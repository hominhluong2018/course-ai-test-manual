# [BUG] Login — Trang đăng nhập phục vụ được qua HTTP thuần, không ép chuyển hướng sang HTTPS và không có HSTS

## Thông tin chung

- **ID:** BUG_login_1785678750_TC039
- **Severity:** 🔴 Critical (nhóm *security* theo bảng phân loại chuẩn)
- **Priority:** P1
- **Môi trường:** Perfex CRM — Anh Tester Demo (`crm.anhtester.com`) | Chrome 150 (Chromium qua Playwright MCP) | Windows 11 Pro 26200 | 1920×1080
- **Test case liên quan:** `CRM_LOGIN_TC_039` — [test_cases_login.md](../../testcases/login/test_cases_login.md) *(sửa ngày 2026-08-20 — file gốc ghi nhầm `CRM_LOGIN_TC_004`, mã đó ở bộ TC hiện tại là "Logo dẫn về trang chủ", không liên quan)*
- **Requirement:** `REQ-LOGIN-01` — Truy cập trang đăng nhập / chuẩn bảo mật HTTPS *(sửa ngày 2026-08-20 — file gốc ghi nhầm `REQ-LOGIN-24`, mã đó ở tài liệu hiện tại là "Trang Quên mật khẩu không có lối quay lại đăng nhập", không liên quan)*
- **Execution run gốc:** `run_1785678326` — thư mục đã mất khi dọn dự án cũ, không còn truy cập được
- **Execution run xác nhận lại:** `run_1787215085` — [execution_report.md](../../executions/login/run_1787215085/execution_report.md)
- **Test data đã dùng:** Không cần tài khoản — lỗi tái hiện ở trạng thái chưa đăng nhập
- **Ngày phát hiện:** 2026-08-02

## Steps to Reproduce

1. Mở trình duyệt ở trạng thái chưa đăng nhập (đã xoá cookie của `crm.anhtester.com`)
2. Nhập vào thanh địa chỉ đúng chuỗi sau (giao thức **http**, không phải https): `http://crm.anhtester.com/admin/authentication`
3. Nhấn Enter và chờ trang tải xong
4. Quan sát thanh địa chỉ của trình duyệt

## Expected Result

Theo `REQ-LOGIN-01` *(sửa ngày 2026-08-20 — trước đó ghi nhầm `REQ-LOGIN-24`)*, trang đăng nhập chỉ được phục vụ trên kênh mã hoá:

- Request HTTP phải bị chuyển hướng (301/308) sang `https://crm.anhtester.com/admin/authentication`
- URL cuối cùng trên thanh địa chỉ có giao thức `https://`
- Trình duyệt hiển thị biểu tượng ổ khoá, không có cảnh báo "Not secure"

## Actual Result

Server phục vụ trang đăng nhập **trực tiếp trên HTTP, không hề chuyển hướng**:

| Kiểm chứng | Kết quả thực tế |
|---|---|
| `GET http://crm.anhtester.com/admin/authentication` | **HTTP 200** (không phải 301/308) |
| Header `Location` của response HTTP | **KHÔNG CÓ** |
| `performance.getEntriesByType('navigation')[0].redirectCount` | **0** |
| `location.protocol` sau khi tải xong | **`http:`** |
| Header `Strict-Transport-Security` trên bản HTTPS | **KHÔNG CÓ** |

Kết quả: form đăng nhập hiển thị đầy đủ và dùng được trên trang HTTP không mã hoá; trình duyệt đánh dấu trang là không an toàn.

**Hệ quả phụ quan sát được:** trên trang HTTP, toàn bộ 6 file font `Inter-*.woff/.woff2` (được nhúng bằng URL `https://`) bị chặn bởi CORS do khác origin (`http://` ↔ `https://`), sinh **12 lỗi console** và làm giao diện mất font chuẩn.

## Evidence

> ⚠️ Hai ảnh gốc bên dưới trỏ tới `run_1785678326` — thư mục này **đã mất** (bị xoá khi dọn dự án cũ), link không còn mở được. Xem ảnh xác nhận lại năm 2026-08-20 ở cuối mục này.

- **Screenshot chính gốc (có thanh địa chỉ, ảnh đã mất):** `docs/executions/login/run_1785678326/evidence/CRM_LOGIN_TC_004_step2_urlbar_http_not_secure.png`
  → Thanh địa chỉ Chrome hiển thị **⚠ "Not secure"** kèm `crm.anhtester.com/admin/authentication`, **không có biểu tượng ổ khoá** — đúng thời điểm form đăng nhập đang hiển thị đầy đủ và dùng được.

- **Screenshot phụ gốc (chỉ vùng nội dung trang, ảnh đã mất):** `docs/executions/login/run_1785678326/evidence/CRM_LOGIN_TC_004_step2_no_https_redirect.png`

- **Screenshot xác nhận lại 2026-08-20:** [TC_039_http_not_upgraded.png](../../executions/login/run_1787215085/evidence/TC_039_http_not_upgraded.png) — thanh địa chỉ vẫn giữ nguyên `http://crm.anhtester.com/admin/authentication`, không chuyển hướng sang HTTPS

- **Kiểm chứng response header:**

```js
// Playwright APIRequestContext, maxRedirects: 0
GET https://crm.anhtester.com/admin/authentication
  → status: 200
  → strict-transport-security: KHÔNG CÓ

GET http://crm.anhtester.com/admin/authentication
  → status: 200          // kỳ vọng: 301/308
  → location: KHÔNG CÓ redirect
```

- **Console log (trích 4/12 lỗi):**

```
[ERROR] Access to font at 'https://crm.anhtester.com/assets/plugins/inter/Inter-Medium.woff2?v=3.19'
        from origin 'http://crm.anhtester.com' has been blocked by CORS policy:
        No 'Access-Control-Allow-Origin' header is present on the requested resource.
[ERROR] Failed to load resource: net::ERR_FAILED @ https://crm.anhtester.com/assets/plugins/inter/Inter-Medium.woff2?v=3.19
[ERROR] Access to font at 'https://crm.anhtester.com/assets/plugins/inter/Inter-Regular.woff2?v=3.19' ... blocked by CORS policy
[ERROR] Failed to load resource: net::ERR_FAILED @ https://crm.anhtester.com/assets/plugins/inter/Inter-Regular.woff2?v=3.19
```

## Ghi chú thêm

**Tần suất tái hiện:** Luôn luôn — 2/2 lần thử.

**Yếu tố giảm nhẹ (đã kiểm chứng, cần ghi rõ để tránh đánh giá sai mức độ):**

Thuộc tính `action` của form login là `https://crm.anhtester.com/admin/authentication` (đã kiểm chứng qua DOM). Nghĩa là khi người dùng bấm Login trên trang HTTP, **bản thân request POST vẫn đi qua HTTPS** — mật khẩu KHÔNG bị truyền plaintext trong kịch bản người dùng bình thường. Đây **không** phải lỗi rò rỉ thông tin đăng nhập trực tiếp.

**Vì sao vẫn xếp Critical:**

Trang login được phục vụ trên kênh không mã hoá thì **toàn bộ HTML của trang đó có thể bị sửa trên đường truyền** (SSL stripping / MITM trên Wi-Fi công cộng, DNS/ARP spoofing). Kẻ tấn công chỉ cần đổi thuộc tính `action` của form sang máy chủ của họ là thu được email + mật khẩu, trong khi người dùng không thấy dấu hiệu bất thường nào ngoài việc thiếu ổ khoá. Việc không có header `Strict-Transport-Security` khiến trình duyệt không tự nâng cấp request lên HTTPS ở các lần truy cập sau, nên lỗ hổng tồn tại lâu dài. Đây là lý do OWASP yêu cầu trang đăng nhập phải được phục vụ qua HTTPS, không chỉ endpoint xử lý.

**Nghi ngờ root cause:** Web server (Nginx/Apache) đang lắng nghe cả cổng 80 và 443 và trả cùng một document root, thiếu rule redirect vĩnh viễn từ HTTP sang HTTPS.

**Đề xuất fix:**

1. Cấu hình web server redirect **301** mọi request `http://` sang `https://` cho toàn site
2. Bật header `Strict-Transport-Security: max-age=31536000; includeSubDomains` trên các response HTTPS
3. Sau khi có redirect, 12 lỗi CORS font sẽ tự hết vì trang và tài nguyên cùng origin `https://`

**Workaround tạm cho người dùng:** luôn gõ đầy đủ `https://` khi truy cập hệ thống.

**Cách verify sau khi fix:** chạy lại `CRM_LOGIN_TC_039` *(sửa ngày 2026-08-20 — trước đó ghi nhầm `CRM_LOGIN_TC_004`)* — kỳ vọng `GET http://crm.anhtester.com/admin/authentication` trả `301` kèm header `Location: https://crm.anhtester.com/admin/authentication`, và `redirectCount = 1`, `location.protocol = "https:"`.

## Lịch sử retest
<!-- Do /retest-fixed-bugs ghi. Dòng mới thêm LÊN ĐẦU bảng. Bảng rỗng = chưa retest lần nào -->

| Ngày | Build | Kết quả | Retest report | Ghi chú |
|---|---|---|---|---|
| 2026-08-20 | Perfex CRM demo tại `crm.anhtester.com` (không rõ version) | ❌ NOT_FIXED | [run_1787215085/execution_report.md](../../executions/login/run_1787215085/execution_report.md) | Đo lại qua `CRM_LOGIN_TC_039` (đúng mã sau khi sửa tham chiếu) — `http://crm.anhtester.com/admin/authentication` vẫn phục vụ trực tiếp, không redirect sang HTTPS. Nhân dịp đo lại cũng sửa các tham chiếu TC ID/REQ ID đã lệch nghĩa từ bản gốc và bổ sung evidence mới (evidence gốc `run_1785678326` đã mất) |
