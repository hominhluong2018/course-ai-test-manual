# Impact Report — `PO-2026-08-18` · Module `LOGIN`

> **Nguồn thay đổi:** quyết định của PO trả lời 4 ambiguity đang treo, ghi nhận trong phiên làm việc ngày 2026-08-18. **Không có file ticket** — mã `PO-2026-08-18` do agent đặt theo ngày để truy vết được đợt quyết định này.
>
> Tài liệu module: [../requirements_login.md](../requirements_login.md) · Danh mục: [../../README.md](../../README.md)

## Quyết định gốc (nguyên văn)

| # | Quyết định |
|---|---|
| 1 | `AMB-02` không khoá |
| 2 | `AMB-04` bỏ qua case này |
| 3 | `AMB-14` chỗ này để cho module khác kiểm chứng |
| 4 | `AMB-15` Remember Me không hoạt động, bỏ qua |

---

## Tóm tắt

| Nhóm | Số lượng | REQ |
|---|---|---|
| 🟢 Thêm mới | 1 | `REQ-LOGIN-41` |
| 🟡 Sửa | 3 | `REQ-LOGIN-27`, `REQ-LOGIN-30`, `REQ-LOGIN-40` |
| 🔴 Bỏ | 0 | — |
| ⏸️ Trùng, không tác động | 0 | — |

**Tổng REQ module:** 40 → **41** · **Phạm vi viết TC: 39 REQ** (2 REQ ⚪ ra ngoài phạm vi).

> Không REQ nào bị 🔴 Deprecated. Hai REQ ra ngoài phạm vi (`27`, `40`) **giữ nguyên dòng và mã** — tính năng chưa bị gỡ khỏi hệ thống, chỉ là không kiểm thử.

---

## Test case cần xử lý

| TC ID | REQ liên quan | Hành động | Lý do |
|---|---|---|---|
| — | `REQ-LOGIN-41` | ➕ **Viết mới** | Yêu cầu mới: không khoá tài khoản sau nhiều lần đăng nhập sai |
| — | `REQ-LOGIN-27` | 🚫 **Không viết** | Ra ngoài phạm vi — `AMB-04` ⏭️ bỏ qua |
| — | `REQ-LOGIN-40` | 🚫 **Không viết** | Ra ngoài phạm vi — `AMB-15` ⏭️ Remember Me không hoạt động |
| — | `REQ-LOGIN-30` | ➕ Viết mới, gắn nhãn `assumption-based` | Bằng chứng dừng ở mức đọc mã nguồn; kiểm chứng đầu-cuối do module `TASK` đảm nhiệm |

> ✅ **Không có TC nào phải sửa hay archive.** Module `LOGIN` **chưa có test case nào** (chưa chạy `/generate_testcases_*`), nên mọi thay đổi ở đợt này đều được hấp thụ ngay khi sinh TC lần đầu.
>
> Kết luận của `AMB-02` **trùng với Giả định tạm** đã ghi trong tài liệu → kể cả khi đã có TC thì cũng không TC nào phải sửa.

---

## Ambiguity

| Mã | Chuyển trạng thái | Ghi chú |
|---|---|---|
| `AMB-02` | ❓ → ✅ Đã trả lời | Không có cơ chế khoá tài khoản (không theo email, không theo IP). Chốt thành `REQ-LOGIN-41`. **Gỡ BLOCKED** cho `STORY-LOGIN-03` |
| `AMB-04` | ❓ → ⏭️ Bỏ qua | Không kiểm chứng phản hồi của Quên mật khẩu với email có thật. `REQ-LOGIN-27` ra ngoài phạm vi |
| `AMB-14` | ❓ → ⏭️ Chuyển module | Kiểm chứng đầu-cuối popup cảnh báo timer giao cho module `TASK` |
| `AMB-15` | ❓ → ⏭️ Bỏ qua | Remember Me không hoạt động. `REQ-LOGIN-40` ra ngoài phạm vi |

**Ambiguity còn treo: 13** (2 🔴 · 10 🟡 · 1 🟢). Không phát sinh ambiguity mới ở đợt này.

## Rủi ro

| Mã | Chuyển trạng thái | Ghi chú |
|---|---|---|
| `RISK-01` | Nghi ngờ → **XÁC NHẬN** | Không CAPTCHA, không giới hạn tần suất, **và xác nhận không khoá tài khoản** → không có lớp chống thử vét cạn nào |
| `RISK-03` | Đang mở → ✅ **ĐÓNG** | Không có cơ chế khoá → không còn nguy cơ khoá cả đội |
| `RISK-04` | Đang mở → ⚠️ **ĐÃ CHẤP NHẬN** | Khả năng lộ email nào có tài khoản sẽ không được xác minh |
| `RISK-05` | Đang mở → ⚠️ **Chấp nhận một phần** | Phần liên kết hợp lệ: chấp nhận. Phần HTTP 500 với mã khoá sai: vẫn cần `AMB-03` |
| `RISK-08` | (mới) | Checkbox Ghi nhớ đăng nhập **vẫn hiển thị và vẫn cấp cookie** dù tính năng không hoạt động |

---

## Cảnh báo & việc còn lại

- ⚠️ **`AMB-03` là ambiguity 🔴 duy nhất còn lại của module** — liên kết đặt lại mật khẩu với mã khoá sai trả **HTTP 500 trắng trang**. Đây là lỗi **người dùng thật chạm được** (bấm liên kết hết hạn trong email), khác hẳn `AMB-04` vốn chỉ là vấn đề kiểm chứng. Quyết định bỏ qua `AMB-04` **không** bao hàm `AMB-03`.
- 🔗 **Phụ thuộc chéo module:** khi recon module `TASK`, phải kiểm chứng đầu-cuối popup `Started tasks timers found!` rồi **cập nhật ngược `REQ-LOGIN-30`**. Không làm thì REQ này vĩnh viễn dừng ở mức bằng chứng đọc mã nguồn.
- 🐞 **Đề xuất báo dev (ngoài phạm vi test):** Remember Me không hoạt động nhưng checkbox vẫn hiển thị, tích được, và vẫn khiến hệ thống cấp cookie `autologin` mang `user_id` + `key` **không có `HttpOnly`** (`RISK-02`). Tức là một tính năng không mang lại lợi ích nào nhưng vẫn tạo bề mặt tấn công. Nên gỡ checkbox và ngừng cấp cookie.
- ✅ **Không Story nào còn BLOCKED** — module sẵn sàng cho bước sinh test case.

---

## Bước kế tiếp

Module `LOGIN` **chưa có test case nào**, nên **không cần** chạy `/update-testcases-from-impact` (workflow đó dành cho bộ TC đã tồn tại).

Chạy thẳng:

```
/generate-testcases-manual-rbt docs/requirements/login/requirements_login.md
```

Khi sinh TC, **bỏ qua** `REQ-LOGIN-27` và `REQ-LOGIN-40` (ngoài phạm vi), và gắn nhãn `assumption-based` cho TC của `REQ-LOGIN-30`.
