# Test Cases — Module Đăng nhập / Xác thực (`LOGIN`) — 41 TC · độ hạt GỘP

| Thông tin | Nội dung |
|---|---|
| **Hệ thống** | Perfex CRM — Anh Tester Demo (`https://crm.anhtester.com`) |
| **Module** | Đăng nhập / Xác thực · prefix `LOGIN` |
| **Nguồn requirement** | [requirements_login.md](../../requirements/login/requirements_login.md) — 43 REQ, **39 trong phạm vi** |
| **Mode sinh** | QUICK (`/generate-testcases-from-requirements`) · **độ hạt GỘP** |
| **Ngày sinh** | 2026-08-20 · **cập nhật ngôn ngữ kiểm chứng 2026-08-20** |
| **Dải TC ID** | `CRM_LOGIN_TC_001` → `CRM_LOGIN_TC_041` — **giữ nguyên qua lần cập nhật này** |
| **Mã kế tiếp** | `CRM_LOGIN_TC_042` — **KHÔNG đánh lại từ 001** |
| **Môi trường** | ⚠️ **Dùng chung** — mọi TC chỉ đọc hoặc hoàn tác được; không có TC nào phá huỷ dữ liệu nghiệp vụ |
| **Trình duyệt chuẩn** | Google Chrome, viewport desktop `1600×750` |
| **Tài khoản** | 🔒 Lấy từ `.env` (`ADMIN_*`, `PM_*`, `CUSTOMER_*`) — **KHÔNG** ghi mật khẩu thật vào tài liệu |

## Cách đọc bộ TC này

### 1. Độ hạt GỘP

TC ở đây được viết theo **độ hạt GỘP**: các biến thể của **cùng một trường** cho **cùng loại phản hồi** nằm chung một TC, liệt kê trong **Bảng biến thể** ở cột `Test Data` (hoặc **Bảng kiểm** ở cột `Expected Result` với TC kiểm tra tĩnh).

- Mỗi biến thể có mã riêng `a`, `b`, `c`… → báo cáo FAIL **bắt buộc** ghi rõ biến thể: `CRM_LOGIN_TC_012-c FAIL`
- Chạy một TC nghĩa là chạy **hết** biến thể của nó, không được bỏ dòng nào
- Sang automation, TC gộp map thành **test data-driven** (`@DataProvider` · `test.each` · `parametrize`), mỗi biến thể một bộ dữ liệu

> Cần mỗi biến thể là một TC riêng (khách hàng tính công theo số TC, hoặc cần pass-rate từng biến thể) → sinh lại bằng `/generate-testcases-from-requirements <đường dẫn> TÁCH`.

### 2. Dòng `🔧 Ghi chú kỹ thuật` — đọc kỹ trước khi chạy

Mọi bước và mọi kết quả kỳ vọng ở phần chính đều **quan sát được bằng mắt trên màn hình**. Tester chạy được toàn bộ bộ TC này mà **không cần mở DevTools**.

Một số yêu cầu — cookie ghi nhớ, mã chống CSRF, header bảo mật, mã trạng thái HTTP — **không có biểu hiện nhìn thấy được**. Phần kiểm chứng cấp kỹ thuật của chúng nằm ở dòng riêng mở đầu bằng:

```
🔧 Ghi chú kỹ thuật (cần DevTools): ...
```

| | |
|---|---|
| **Ai chạy phần chính** | Tester nghiệp vụ — chỉ dùng trình duyệt |
| **Ai chạy dòng 🔧** | Tester có kỹ năng DevTools, hoặc để dành cho automation |
| **TC có dòng 🔧** | Gắn tag **`@TechCheck`** — xem bộ chạy riêng ở cuối tài liệu |
| **Bỏ qua dòng 🔧 được không** | ✅ Được. TC vẫn chấm PASS/FAIL bình thường ở phần chính. Khi bỏ qua thì ghi `— (bỏ phần 🔧)` vào báo cáo |

🔒 Dòng 🔧 chỉ ghi **hình thái** giá trị bí mật (`<16 ký tự hex>`), **KHÔNG** chép giá trị thật vào báo cáo.

---

## Nhóm A — Giao diện & cấu trúc trang đăng nhập

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automatable | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_001 | REQ-LOGIN-01 | LOGIN | High | Mở trang đăng nhập khi chưa có phiên thì thấy biểu mẫu đăng nhập | Trình duyệt sạch, chưa từng đăng nhập vào `crm.anhtester.com` (dùng cửa sổ ẩn danh cho chắc) | 1. Mở `https://crm.anhtester.com/admin/authentication`<br>2. Đọc URL trên thanh địa chỉ<br>3. Đọc chữ trên tab trình duyệt<br>4. Nhìn tiêu đề hiển thị trong khối biểu mẫu | URL: `https://crm.anhtester.com/admin/authentication` | 1. Trang tải xong bình thường, không hiện trang lỗi<br>2. Thanh địa chỉ dừng đúng ở `https://crm.anhtester.com/admin/authentication`, không bị đẩy đi đâu khác<br>3. Tab trình duyệt ghi `Perfex CRM \| Anh Tester Demo - Login`<br>4. Khối biểu mẫu có tiêu đề `Login`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): request tài liệu trả HTTP `200`; `body` mang class `login_admin` | Critical | Yes | UI | @Smoke, @CriticalPath, @TechCheck, @Login |
| CRM_LOGIN_TC_002 | REQ-LOGIN-02, REQ-LOGIN-03 | LOGIN | High | Biểu mẫu đăng nhập đủ thành phần và đúng trạng thái mặc định | Đang ở `/admin/authentication`, trang vừa nạp, **chưa nhập gì và chưa bấm gì** | 1. Mở `/admin/authentication`, KHÔNG bấm chuột vào đâu<br>2. Đối chiếu lần lượt từng mục trong Bảng kiểm ở cột Expected Result<br>3. Riêng mục `5` thì gõ `Abc12345` vào ô Password rồi quan sát<br>4. Riêng mục `6` thì bấm vào **chữ** `Remember me` (không bấm ô vuông) | Password gõ ở mục `5`: `Abc12345` | **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-02) Nhìn thấy đủ 5 thành phần: ô nhập nhãn `Email Address` · ô nhập nhãn `Password` · ô vuông tích nhãn `Remember me` · nút `Login` · liên kết `Forgot Password?` nằm **dưới** nút Login<br>`2` (REQ-03) Con trỏ nhấp nháy sẵn trong ô Email — gõ được ngay, **không** cần bấm chuột vào ô<br>`3` (REQ-02) Ô vuông `Remember me` đang **rỗng**, viền xám **rõ nét** — KHÔNG xám mờ (xám mờ nghĩa là bị khoá, khác hẳn)<br>`4` (REQ-02) Nút `Login` nền xanh đậm chữ trắng, **bấm được** kể cả khi hai ô còn rỗng<br>`5` (REQ-02) Gõ vào ô Password thì hiện 8 dấu chấm tròn, **không** hiện chữ<br>`6` (REQ-02) Bấm vào **chữ** `Remember me` thì ô vuông chuyển sang tích ✔ (nhãn ăn khớp với ô)<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): `<form>` có `action` = `https://crm.anhtester.com/admin/authentication`, `method` = `post`; `input#email` mang `autofocus="1"`; ba `<label>` có `for` = `email` / `password` / `remember` khớp `id` | Critical | Yes | UI | @Smoke, @CriticalPath, @Accessibility, @TechCheck, @Login |
| CRM_LOGIN_TC_003 | REQ-LOGIN-05, REQ-LOGIN-36, REQ-LOGIN-37 | LOGIN | Medium | Trang đăng nhập không có CAPTCHA, không đăng nhập bên thứ ba, không nạp JavaScript | Đang ở `/admin/authentication`, trang vừa nạp, chưa gửi biểu mẫu lần nào | 1. Mở `/admin/authentication` và nạp lại một lần<br>2. Nhìn khắp trang, đối chiếu lần lượt từng mục trong Bảng kiểm | — (chỉ quan sát) | **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-05) **Không** thấy ô "Tôi không phải người máy", **không** thấy ảnh đố chọn hình, **không** thấy khối CAPTCHA nào<br>`2` (REQ-36) Trên trang chỉ có **một** nút gửi duy nhất là `Login`. **Không** thấy nút `Sign in with Google` / `Continue with Facebook` / Microsoft hay bất kỳ nút đăng nhập bên thứ ba nào<br>`3` (REQ-37) Trang hiển thị đầy đủ và bấm được ngay khi vừa tải — không có hiệu ứng chờ tải kịch bản<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): số phần tử `.g-recaptcha` = `0`, `[class*=captcha]` = `0`, `iframe[src*=recaptcha]` = `0`; toàn trang đúng **1** `<form>` và **1** `button[type=submit]`; **0** `<script>` nội tuyến và **0** `<script src>`; tab Network lọc loại `JS` không ghi nhận request `.js` nào | Medium | Yes | UI | @Regression, @Security, @TechCheck, @Login |
| CRM_LOGIN_TC_004 | REQ-LOGIN-04 | LOGIN | Low | Bấm logo trên trang đăng nhập thì về trang chủ công khai | Đang ở `/admin/authentication` | 1. Bấm vào logo `ANHTESTER` phía trên khối biểu mẫu<br>2. Đợi trang chuyển xong<br>3. Đọc URL trên thanh địa chỉ | — (chỉ điều hướng) | 2. Trang chuyển sang trang chủ công khai<br>3. Thanh địa chỉ dừng ở `https://crm.anhtester.com/` — không còn ở khu `/admin` | Low | Yes | UI | @Regression, @Login |

---

## Nhóm B — Đăng nhập thành công & Ghi nhớ đăng nhập

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automatable | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_005 | REQ-LOGIN-06 | LOGIN | High | Đăng nhập bằng tài khoản Admin hợp lệ thì vào được Dashboard | Chưa đăng nhập, đang ở `/admin/authentication` | 1. Nhập ô Email: `admin@example.com`<br>2. Nhập ô Password bằng giá trị `ADMIN_PASSWORD` trong `.env`<br>3. Để trống ô tích `Remember me`<br>4. Bấm nút `Login`<br>5. Đợi trang chuyển xong<br>6. Đọc URL, đọc chữ trên tab trình duyệt, nhìn nội dung trang | Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env` | 4. Trang bắt đầu chuyển, **không** hiện dải báo lỗi đỏ nào<br>5. Chuyển xong, không quay lại trang đăng nhập<br>6. Dừng ở `https://crm.anhtester.com/admin/` · tab trình duyệt có chữ `Dashboard` · trang hiện menu điều hướng bên trái và ảnh đại diện góc trên bên phải<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): `POST /admin/authentication` trả `303` với header `location: https://crm.anhtester.com/admin/`; `body` mang đủ 4 class `app`, `admin`, `dashboard`, `user-id-2` | Critical | Yes | UI | @Smoke, @CriticalPath, @TechCheck, @Login |
| CRM_LOGIN_TC_006 | REQ-LOGIN-06 | LOGIN | High | Đăng nhập bằng tài khoản Project Manager thì vào Dashboard với bộ menu rút gọn | Chưa đăng nhập, đang ở `/admin/authentication` | 1. Nhập Email bằng giá trị `PM_EMAIL` trong `.env`<br>2. Nhập Password bằng giá trị `PM_PASSWORD` trong `.env`<br>3. Bấm nút `Login`<br>4. Đọc URL sau khi trang chuyển xong<br>5. Đếm số mục ở menu điều hướng bên trái<br>6. Rà menu trái tìm 5 mục `Subscriptions`, `Expenses`, `Estimate Request`, `Knowledge Base`, `Reports` | Email: giá trị `PM_EMAIL` trong `.env`<br>Password: giá trị `PM_PASSWORD` trong `.env` | 3. Đăng nhập thành công, không dải báo lỗi đỏ nào<br>4. Dừng ở `https://crm.anhtester.com/admin/`<br>5. Menu trái đếm được đúng **9** mục (Admin có 14 — đối chiếu với `CRM_LOGIN_TC_005`)<br>6. **Không** mục nào trong 5 mục kể trên xuất hiện<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): `body` mang class `user-id-3` | High | Yes | UI | @Regression, @Permission, @TechCheck, @Login |
| CRM_LOGIN_TC_007 | REQ-LOGIN-07, REQ-LOGIN-38 | LOGIN | Medium | Email được chuẩn hoá — khác kiểu chữ và thừa khoảng trắng vẫn đăng nhập được | Chưa đăng nhập, đang ở `/admin/authentication`. Chạy lại từ bước 1 cho **từng** biến thể (đăng xuất giữa các lượt) | 1. Nhập ô Email theo biến thể<br>2. Nhập ô Password bằng giá trị `ADMIN_PASSWORD` trong `.env`<br>3. Bấm nút `Login`<br>4. Đọc URL và chữ trên tab trình duyệt | **Bảng biến thể** (ký hiệu `·` = 1 dấu cách):<br>`a` Lẫn hoa thường → `ADMIN@Example.COM`<br>`b` Toàn chữ HOA → `ADMIN@EXAMPLE.COM`<br>`c` 2 dấu cách mỗi đầu → `··admin@example.com··`<br>`d` 1 dấu cách cuối → `admin@example.com·`<br><br>Password mọi biến thể: giá trị `ADMIN_PASSWORD` trong `.env` | **Mọi biến thể cho cùng kết quả:**<br>3. **KHÔNG** hiện dải báo lỗi `Invalid email or password`<br>4. Dừng ở `https://crm.anhtester.com/admin/`, tab trình duyệt có chữ `Dashboard`<br><br>`a`,`b` chứng minh không phân biệt hoa/thường (REQ-07) · `c`,`d` chứng minh bỏ qua khoảng trắng đầu/cuối (REQ-38) | High | Yes | UI | @Regression, @Boundary, @Login |
| CRM_LOGIN_TC_008 | REQ-LOGIN-08 | LOGIN | High | Tích Ghi nhớ đăng nhập thì hệ thống phát hành cookie ghi nhớ | **Trạng thái sạch bắt buộc:** đã đăng xuất và đã xoá toàn bộ cookie của `crm.anhtester.com` (Cài đặt trình duyệt → Xoá dữ liệu duyệt web cho riêng trang này) | 1. Mở `/admin/authentication`<br>2. Nhập Email `admin@example.com`<br>3. Nhập Password bằng giá trị `ADMIN_PASSWORD` trong `.env`<br>4. Bấm ô tích `Remember me` cho tới khi hiện dấu tích ✔<br>5. Bấm nút `Login`<br>6. Thực hiện phần 🔧 nếu có người chạy được | Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env`<br>Remember me: **tích** | 4. Ô vuông chuyển sang tích ✔ rõ ràng<br>5. Đăng nhập thành công, dừng ở `https://crm.anhtester.com/admin/`, không dải báo lỗi nào<br><br>⚠️ **Việc phát hành cookie ghi nhớ không có biểu hiện nhìn thấy được.** Hệ quả nghiệp vụ (đóng trình duyệt mở lại vẫn đăng nhập) **KHÔNG** dùng làm kỳ vọng ở đây — `REQ-LOGIN-40` ghi nhận tính năng đó **không hoạt động** (`AMB-15` ⏭️). Vì vậy phần chính chỉ chấm được tới bước 5; kiểm chứng thật nằm ở dòng 🔧<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): sau khi dừng ở `/admin/`, mở Application → Cookies. Trước bước 1 danh sách cookie phải **rỗng**; sau bước 5 phải tồn tại cookie `autologin` khớp hình thái `a:2:{s:7:"user_id";s:1:"<id>";s:3:"key";s:16:"<16 ký tự hex>";}` — phần `key` dài đúng **16** ký tự hex. 🔒 Chỉ ghi hình thái và độ dài, **KHÔNG** chép giá trị thật vào báo cáo | High | Partial | UI | @Regression, @Security, @TechCheck, @Login |
| CRM_LOGIN_TC_009 | REQ-LOGIN-09 | LOGIN | High | Không tích Ghi nhớ đăng nhập thì hoàn toàn không phát hành cookie ghi nhớ | Chưa đăng nhập | 1. Xoá toàn bộ cookie của `crm.anhtester.com` trong cài đặt trình duyệt<br>2. Mở `https://crm.anhtester.com/admin/authentication/logout` để kết thúc mọi phiên còn sót<br>3. Mở `/admin/authentication`, xác nhận ô `Remember me` đang **rỗng**<br>4. Nhập Email `admin@example.com` và Password bằng `ADMIN_PASSWORD` trong `.env`, **KHÔNG** động vào ô tích<br>5. Bấm `Login`<br>6. Thực hiện phần 🔧 nếu có người chạy được | Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env`<br>Remember me: **để trống** | 3. Ô `Remember me` rỗng, viền xám rõ nét<br>5. Đăng nhập thành công, dừng ở `https://crm.anhtester.com/admin/`<br><br>⚠️ Thứ tự 3 bước tiền đề là **bắt buộc**: bỏ bước xoá cookie thì không phân biệt được *không cấp* với *cấp lại trùng giá trị cũ*<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): sau bước 3 danh sách cookie phải **rỗng**; sau bước 5 vẫn phải **rỗng** — **không** tồn tại cookie `autologin` | High | Partial | UI | @Regression, @Security, @TechCheck, @Login |
| CRM_LOGIN_TC_010 | REQ-LOGIN-06 | LOGIN | Medium | Bấm nút Login hai lần liên tiếp vẫn vào đúng Dashboard, không sinh trang lỗi | Chưa đăng nhập, đang ở `/admin/authentication` | 1. Nhập Email `admin@example.com` và Password bằng `ADMIN_PASSWORD` trong `.env`<br>2. Bấm nút `Login` hai lần liên tiếp trong vòng dưới 1 giây<br>3. Đợi trang dừng chuyển hướng<br>4. Đọc URL, nhìn có dải báo lỗi đỏ nào không<br>5. Đọc toàn bộ chữ trên trang xem có dòng `419 Page Expired!` không | Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env` | 3. Trang dừng chuyển hướng, không treo, không quay vòng<br>4. Dừng ở `https://crm.anhtester.com/admin/`, tab trình duyệt có chữ `Dashboard`, **không** dải báo lỗi đỏ nào<br>5. **Không** xuất hiện dòng `419 Page Expired!` | Medium | Partial | UI | @Regression, @RaceCondition, @Login |

---

## Nhóm C — Kiểm tra dữ liệu đầu vào & Đăng nhập thất bại

> 🔓 **Thử sai mật khẩu lặp lại là an toàn** — `REQ-LOGIN-41` đã chốt hệ thống không có cơ chế khoá tài khoản (`AMB-02` ✅, `RISK-03` đã đóng). Vẫn ưu tiên email không tồn tại khi chỉ cần lấy thông báo lỗi, vì đây là **môi trường dùng chung**.
>
> 🚫 **KHÔNG viết TC biên cho trường Password** ở bất kỳ màn hình nào — `AMB-09` chốt hệ thống không quy định chính sách mật khẩu nào.

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automatable | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_011 | REQ-LOGIN-10, REQ-LOGIN-11, REQ-LOGIN-12 | LOGIN | High | Kiểm tra trường bắt buộc trên biểu mẫu đăng nhập | Đang ở `/admin/authentication`. Nạp lại trang trước **mỗi** biến thể | 1. Nhập Email và Password theo biến thể<br>2. Bấm nút `Login`<br>3. Đếm số dải báo lỗi đỏ hiện trên trang<br>4. Đọc nội dung từng dải theo thứ tự trên xuống<br>5. Đọc URL trên thanh địa chỉ | **Bảng biến thể** (ký hiệu `·` = 1 dấu cách):<br>`a` ⭐smoke Rỗng cả hai → Email: *(trống)* · Password: *(trống)*<br>`b` Rỗng riêng Email → Email: *(trống)* · Password: `Abc12345`<br>`c` Rỗng riêng Password → Email: `admin@example.com` · Password: *(trống)*<br>`d` Toàn khoảng trắng → Email: `···` · Password: `···` | **Mọi biến thể:** trang nạp lại, **KHÔNG** vào được Dashboard, vẫn ở `https://crm.anhtester.com/admin/authentication`<br><br>`a` → đúng **2** dải, thứ tự từ trên xuống: `The Password field is required.` rồi `The Email Address field is required.`<br>`b` → **duy nhất 1** dải `The Email Address field is required.`, KHÔNG kèm `Invalid email or password`<br>`c` → **duy nhất 1** dải `The Password field is required.`, KHÔNG kèm `Invalid email or password`<br>`d` → như `a` (hệ thống cắt khoảng trắng trước khi kiểm trường bắt buộc). ⚠️ Biến thể `d` **chưa có evidence** — ra `Invalid email or password` thì ghi nhận hiện trạng và mở ambiguity mới | Critical | Yes | UI | @Smoke, @Regression, @NeedsVerify, @Login |
| CRM_LOGIN_TC_012 | REQ-LOGIN-13 | LOGIN | High | Email sai định dạng bị trình duyệt chặn ngay, biểu mẫu không gửi đi | Đang ở `/admin/authentication`. Nạp lại trang trước **mỗi** biến thể | 1. Nhập ô Email theo biến thể<br>2. Nhập ô Password: `Abc12345`<br>3. Bấm nút `Login`<br>4. Quan sát khu vực ô Email<br>5. Quan sát trang có nạp lại không và dữ liệu hai ô còn không | **Bảng biến thể:**<br>`a` Thiếu `@` → `abc`<br>`b` Thiếu tên miền → `abc@`<br>`c` Thiếu phần trước `@` → `@example.com`<br>`d` Nhiều `@` → `a@b@example.com`<br>`e` Chuỗi tiêm SQL → `admin@example.com' OR '1'='1`<br><br>Password mọi biến thể: `Abc12345` | **Mọi biến thể cho cùng kết quả:**<br>4. Trình duyệt bật bong bóng cảnh báo ngay cạnh ô Email — ⚠️ nội dung bong bóng do **trình duyệt** sinh, đổi theo trình duyệt và ngôn ngữ hệ điều hành, **CẤM dùng làm điều kiện chấm PASS/FAIL**. Chỉ chấm là *có* bong bóng hay không<br>5. Trang **không** nạp lại, chữ đã nhập ở cả hai ô còn nguyên, **không** hiện dải báo lỗi của hệ thống<br><br>Biến thể `e` chứng minh thêm: chuỗi tiêm SQL bị chặn tại trình duyệt, không tới được máy chủ<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): tab Network **không** ghi nhận request `POST /admin/authentication` nào | High | Yes | UI | @Regression, @Boundary, @Security, @TechCheck, @Login |
| CRM_LOGIN_TC_013 | REQ-LOGIN-14, REQ-LOGIN-15 | LOGIN | High | Sai thông tin đăng nhập trả về một thông báo chung, không tiết lộ email nào có thật | Đang ở `/admin/authentication`, chưa đăng nhập. Nạp lại trang giữa hai biến thể | 1. Nhập Email và Password theo biến thể<br>2. Bấm nút `Login`<br>3. Ghi lại: nội dung dải báo lỗi · số lượng dải · URL trên thanh địa chỉ<br>4. Lặp bước 1–3 cho biến thể còn lại<br>5. **So sánh** hai bộ kết quả ở bước 3 | **Bảng biến thể:**<br>`a` ⭐smoke Email có thật, mật khẩu sai → `admin@example.com` / `SaiMatKhau_20260820`<br>`b` Email không tồn tại → `notexist_20260820@auto.test` / `BatKyMatKhau@123` | **Mỗi biến thể:** trang nạp lại, **KHÔNG** vào được Dashboard; đúng **1** dải báo lỗi, nội dung nguyên văn `Invalid email or password`; vẫn ở `https://crm.anhtester.com/admin/authentication`<br><br>5. Hai bộ kết quả **giống hệt nhau** ở cả 3 tiêu chí (nội dung dải · số lượng dải · URL) — không dấu hiệu nào phân biệt email có thật với email không tồn tại (REQ-15) | Critical | Yes | UI | @Smoke, @Regression, @Security, @Login |
| CRM_LOGIN_TC_014 | REQ-LOGIN-14 | LOGIN | Medium | Mật khẩu đúng nhưng sai kiểu chữ hoa/thường thì bị từ chối | Đang ở `/admin/authentication`, chưa đăng nhập | 1. Nhập ô Email: `admin@example.com`<br>2. Nhập ô Password bằng giá trị `ADMIN_PASSWORD` trong `.env` nhưng **đảo kiểu chữ** mọi ký tự chữ cái<br>3. Bấm nút `Login`<br>4. Đọc dải báo lỗi và URL | Email: `admin@example.com`<br>Password: `ADMIN_PASSWORD` đã đảo hoa/thường | 3. Trang nạp lại, **không** vào được Dashboard<br>4. Dải báo lỗi ghi `Invalid email or password`, vẫn ở `https://crm.anhtester.com/admin/authentication`<br><br>⚠️ **Chưa chạy được trên tài khoản demo hiện tại** — mật khẩu demo là **6 ký tự số** (`RISK-07`), không có chữ cái để đảo. Cần tài khoản test có mật khẩu chứa chữ cái | Medium | Yes | UI | @Regression, @Security, @NeedsVerify, @Login |
| CRM_LOGIN_TC_015 | REQ-LOGIN-41 | LOGIN | High | Đăng nhập sai nhiều lần liên tiếp không làm khoá tài khoản và không chặn theo IP | Đang ở `/admin/authentication`. Đã xác nhận hệ thống không có cơ chế khoá nên thao tác này an toàn trên môi trường dùng chung | 1. Gửi biểu mẫu sai theo biến thể, đủ số lần quy định<br>2. Sau mỗi lần, đọc nội dung dải báo lỗi<br>3. Sau lần cuối, đọc toàn bộ chữ trên trang tìm các từ `locked`, `blocked`, `too many`, `try again in`<br>4. Ngay sau đó nhập `admin@example.com` + giá trị `ADMIN_PASSWORD` trong `.env`<br>5. Bấm `Login`, đọc URL sau khi trang chuyển xong | **Bảng biến thể:**<br>`a` Khoá theo email — **6 lần** cùng `admin@example.com` với mật khẩu `SaiMatKhau_20260820`<br>`b` Khoá theo IP — **5 lần** với 5 email khác nhau `notexist_a_20260820@auto.test` → `notexist_e_20260820@auto.test`, cùng mật khẩu `Sai@123`<br><br>Cặp đúng ở bước 4: `admin@example.com` + `ADMIN_PASSWORD` trong `.env` | **Mọi biến thể:**<br>2. Mọi lần đều trả đúng 1 dải `Invalid email or password`, nội dung **không đổi** qua các lần — không có đếm ngược, không có cảnh báo còn mấy lần<br>3. **Không** tìm thấy từ nào trong danh sách; không thông báo khoá/hạn chế nào<br>5. Đăng nhập **thành công**, dừng ở `https://crm.anhtester.com/admin/`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): không phản hồi nào trả HTTP `429` | High | Yes | UI | @Regression, @Security, @TechCheck, @Login |
| CRM_LOGIN_TC_016 | REQ-LOGIN-16 | LOGIN | High | 🐞 Sau khi đăng nhập thất bại, ô Email phải còn giữ email vừa nhập | Đang ở `/admin/authentication`. **Bắt buộc chạy trong cửa sổ ẩn danh và tắt tính năng tự điền của trình duyệt** — nếu không, trình duyệt tự điền lại và TC cho kết quả PASS giả | 1. Mở cửa sổ ẩn danh, tắt tự điền, vào `/admin/authentication`<br>2. Nhập ô Email: `admin@example.com`<br>3. Nhập ô Password: `SaiMatKhau_20260820`<br>4. Bấm nút `Login`<br>5. Sau khi trang nạp lại, nhìn ô Email | Email: `admin@example.com`<br>Password: `SaiMatKhau_20260820` | 4. Hiện dải báo lỗi `Invalid email or password`<br>5. Ô Email **phải** còn hiển thị `admin@example.com` — người dùng không phải gõ lại<br><br>🐞 **Hiện trạng: FAIL** — trong cửa sổ ẩn danh ô Email trở về **rỗng**. PO xác nhận đây là **lỗi hệ thống** (`AMB-10`). FAIL là kết quả **đúng** → mở bug, **KHÔNG** sửa TC.<br>⚠️ Chạy ở cửa sổ thường sẽ tưởng đã đạt vì trình duyệt tự điền lại (nền xanh nhạt trong `login_form_wrong_credentials_fullpage.png`) — đó là trình duyệt điền, không phải hệ thống trả về<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): xem HTML **do máy chủ trả về** (View Source, không phải DOM sau xử lý) — thẻ `input#email` phải mang `value="admin@example.com"`. Hiện trạng đo được: không có thuộc tính `value` | High | Yes | UI | @Regression, @KnownBug, @TechCheck, @Login |
| CRM_LOGIN_TC_017 | REQ-LOGIN-14 | LOGIN | High | Chuỗi tấn công nhập vào ô Password không vượt qua được xác thực và không được thực thi | Đang ở `/admin/authentication`. Nạp lại trang trước **mỗi** biến thể | 1. Nhập ô Email theo biến thể<br>2. Nhập ô Password đúng chuỗi tấn công của biến thể<br>3. Bấm nút `Login`<br>4. Quan sát có hộp thoại bật lên giữa màn hình không<br>5. Đọc dải báo lỗi và URL<br>6. Đọc toàn bộ chữ trên trang tìm dấu vết lỗi cơ sở dữ liệu | **Bảng biến thể:**<br>`a` XSS → Email `notexist_20260820@auto.test` · Password `<script>alert(1)</script>`<br>`b` Tiêm SQL → Email `admin@example.com` · Password `' OR 1=1--` | **Mọi biến thể:**<br>3. Trang nạp lại, **không** vào được Dashboard<br>4. **Không** hộp thoại nào bật lên<br>5. Dải báo lỗi ghi `Invalid email or password`, vẫn ở `https://crm.anhtester.com/admin/authentication`<br>6. **Không** thấy thông báo lỗi cơ sở dữ liệu, không thấy đoạn mã lạ hiển thị ra màn hình<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): phản hồi trả HTTP `200` (KHÔNG phải `500`); chuỗi tấn công nếu xuất hiện trong HTML phải ở dạng đã mã hoá (`&lt;script&gt;`), không phải thẻ `<script>` sống | Critical | Yes | UI | @Regression, @Security, @TechCheck, @Login |
| CRM_LOGIN_TC_018 | REQ-LOGIN-14 | LOGIN | Medium | Dữ liệu bất thường không làm hệ thống lỗi máy chủ | Đang ở `/admin/authentication`. Nạp lại trang trước **mỗi** biến thể.<br>📌 TC này kiểm **độ bền** (hệ thống không sập), KHÔNG kiểm validate từng trường — nên hai trường khác nhau được gộp: dấu hiệu FAIL là trang lỗi máy chủ, và bảng biến thể chỉ ngay ra input gây lỗi | 1. Nhập Email và Password theo biến thể<br>2. Bấm nút `Login`<br>3. Quan sát trang trả về<br>4. Đọc nội dung dải báo lỗi và kiểm ký tự hiển thị có đúng không | **Bảng biến thể:**<br>`a` Email rất dài → 250 ký tự `a` liền nhau + `@auto.test` (tổng 260 ký tự) · Password `Abc12345`<br>`b` Password đa ngôn ngữ → Email `notexist_20260820@auto.test` · Password `MậtKhẩuTiếngViệt😀🎉` | **Mọi biến thể:**<br>3. Trang nạp lại bình thường về đúng trang đăng nhập — **không** hiện trang lỗi máy chủ, không trang trắng<br>4. Dải báo lỗi ghi `Invalid email or password`, hiển thị đúng tiếng Anh, không vỡ ký tự, không hiện dấu hỏi / ô vuông lạ<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): phản hồi trả HTTP `200` — **không** có `500` | Medium | Yes | UI | @Regression, @Boundary, @Localization, @TechCheck, @Login |
| CRM_LOGIN_TC_019 | REQ-LOGIN-43, REQ-LOGIN-15 | LOGIN | High | Tài khoản khách hàng hợp lệ bị từ chối ở khu quản trị mà không lộ ra rằng email đó có tài khoản | Chưa đăng nhập, đang ở `/admin/authentication`. Ba bước phải chạy **nối tiếp** với **cùng** một cặp thông tin đăng nhập | 1. Ở `/admin/authentication`, nhập `CUSTOMER_EMAIL` + `CUSTOMER_PASSWORD` trong `.env`, bấm `Login`; ghi lại nội dung dải báo lỗi, số lượng dải, URL<br>2. Mở `https://crm.anhtester.com/login`, nhập **đúng cặp đó**, bấm đăng nhập; đọc URL và chữ trên tab trình duyệt<br>3. Quay lại `/admin/authentication`, gửi `notexist_20260820@auto.test` + `BatKyMatKhau@123`; ghi lại nội dung dải, số lượng dải, URL<br>4. **So sánh** kết quả bước 1 với bước 3 | Cặp Customer: `CUSTOMER_EMAIL` / `CUSTOMER_PASSWORD` trong `.env`<br>Cặp đối chứng: `notexist_20260820@auto.test` / `BatKyMatKhau@123` | 1. **Không** vào được `/admin/`; đúng **1** dải `Invalid email or password`; vẫn ở `https://crm.anhtester.com/admin/authentication`<br>2. Đăng nhập **thành công** ở cổng khách hàng — dừng ở `https://crm.anhtester.com/`, tab trình duyệt ghi `HKD Anh Tester`. Bước này **loại trừ** khả năng sai mật khẩu: cặp thông tin ở bước 1 là đúng, bị từ chối do ranh giới hệ thống<br>3. Đúng 1 dải `Invalid email or password`, vẫn ở `/admin/authentication`<br>4. Kết quả bước 1 và bước 3 **giống hệt nhau** — không lộ ra rằng email này có tài khoản ở hệ thống khách hàng (REQ-15)<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): sau bước 1, kiểm Application → Cookies xác nhận **không** phiên khu quản trị nào được tạo | High | Yes | UI | @Regression, @Security, @Permission, @TechCheck, @Login |
| CRM_LOGIN_TC_020 | REQ-LOGIN-43 | LOGIN | High | Đang có phiên cổng khách hàng hợp lệ vẫn không mở được URL nội bộ khu quản trị | Đã đăng nhập thành công ở `/login` bằng tài khoản Customer (nối tiếp `CRM_LOGIN_TC_019` bước 2), phiên còn hiệu lực | 1. Mở `https://crm.anhtester.com/` xác nhận đang đăng nhập ở cổng khách hàng<br>2. Gõ thẳng `https://crm.anhtester.com/admin/clients` vào thanh địa chỉ<br>3. Đọc URL sau khi trang dừng<br>4. Quan sát nội dung trang | URL đích: `https://crm.anhtester.com/admin/clients` | 1. Phiên cổng khách hàng đang hoạt động<br>3. Dừng ở `https://crm.anhtester.com/admin/authentication`<br>4. Hiển thị biểu mẫu đăng nhập khu quản trị — **không** thấy danh sách khách hàng, không thấy dữ liệu nghiệp vụ nào<br><br>📌 Với khu `/admin`, `Customer` hành xử **y hệt khách chưa đăng nhập**, không phải một cấp quyền thấp hơn | High | Yes | UI | @Regression, @Security, @Permission, @Login |

---

## Nhóm D — Bảo vệ phiên, điều hướng & chống CSRF

> ⚠️ **Chỉ chấm theo điểm dừng nhìn thấy trên thanh địa chỉ**, KHÔNG chấm theo mã chuyển hướng — đợt khảo sát ghi nhận `307`, giá trị bất thường và chưa loại trừ được khả năng do công cụ ghi nhận (`AMB-17`).
>
> ⚠️ Với CSRF: chấm theo **chuỗi chữ `419 Page Expired!` hiện trên màn hình**, KHÔNG chấm theo mã HTTP — header thật trả `403` nhưng nội dung ghi `419`, hai giá trị lệch nhau và chưa được sửa (`AMB-08`).

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automatable | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_021 | REQ-LOGIN-17 | LOGIN | High | Mở thẳng URL nội bộ khi chưa đăng nhập thì bị đưa về trang đăng nhập | Trình duyệt sạch, chưa đăng nhập (dùng cửa sổ ẩn danh cho chắc) | 1. Xác nhận đang ở trạng thái chưa đăng nhập<br>2. Gõ thẳng URL của biến thể vào thanh địa chỉ<br>3. Đợi trang dừng chuyển hướng<br>4. Đọc URL và chữ trên tab trình duyệt<br>5. Quan sát nội dung trang | **Bảng biến thể:**<br>`a` ⭐smoke Danh sách khách hàng → `https://crm.anhtester.com/admin/clients`<br>`b` Trang Dashboard → `https://crm.anhtester.com/admin/` | **Mọi biến thể cho cùng kết quả:**<br>3. Trang tự chuyển hướng<br>4. Dừng ở `https://crm.anhtester.com/admin/authentication`, tab trình duyệt ghi `Perfex CRM \| Anh Tester Demo - Login`<br>5. Hiển thị biểu mẫu đăng nhập — **không** thoáng thấy nội dung nghiệp vụ nào, dù chỉ trong tích tắc | Critical | Yes | UI | @Smoke, @CriticalPath, @Security, @Login |
| CRM_LOGIN_TC_022 | REQ-LOGIN-18 | LOGIN | Medium | Bị chuyển về trang đăng nhập thì không giữ lại URL đích ban đầu | Trình duyệt sạch, chưa đăng nhập | 1. Gõ thẳng `https://crm.anhtester.com/admin/clients`<br>2. Đợi trang dừng chuyển hướng<br>3. Đọc **toàn bộ** URL trên thanh địa chỉ, kể cả phần sau dấu `?`<br>4. Nhập Email `admin@example.com` + Password `ADMIN_PASSWORD` trong `.env`, bấm `Login`<br>5. Đọc URL và chữ trên tab trình duyệt | URL đích: `https://crm.anhtester.com/admin/clients`<br>Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env` | 3. URL đúng bằng `https://crm.anhtester.com/admin/authentication` — **trần**, không có `?redirect=`, `?return_url=`, `?next=` hay tham số nào phía sau<br>5. Dừng ở `https://crm.anhtester.com/admin/` với tab trình duyệt có chữ `Dashboard` — **không** tự quay lại `/admin/clients` | Medium | Yes | UI | @Regression, @Login |
| CRM_LOGIN_TC_023 | REQ-LOGIN-19, REQ-LOGIN-20 | LOGIN | Medium | Đang có phiên thì mở trang đăng nhập hoặc Quên mật khẩu đều bị đưa về Dashboard | Đã đăng nhập bằng vai trò của biến thể, đang ở `/admin/` | 1. Đăng nhập bằng tài khoản của biến thể<br>2. Gõ thẳng URL của biến thể vào thanh địa chỉ<br>3. Đợi trang dừng chuyển hướng<br>4. Đọc URL và chữ trên tab trình duyệt<br>5. Kiểm biểu mẫu (đăng nhập hoặc Quên mật khẩu) có hiển thị không | **Bảng biến thể:**<br>`a` Admin × trang đăng nhập → `admin@example.com` · `/admin/authentication`<br>`b` Admin × Quên mật khẩu → `admin@example.com` · `/admin/authentication/forgot_password`<br>`c` PM × trang đăng nhập → `PM_EMAIL` trong `.env` · `/admin/authentication`<br>`d` PM × Quên mật khẩu → `PM_EMAIL` trong `.env` · `/admin/authentication/forgot_password`<br><br>Mật khẩu lấy từ `.env` theo vai trò tương ứng | **Mọi biến thể cho cùng kết quả:**<br>3. Trang tự chuyển hướng<br>4. Dừng ở `https://crm.anhtester.com/admin/`, tab trình duyệt có chữ `Dashboard`<br>5. **Không** hiển thị biểu mẫu nào<br><br>`a`,`c` phủ REQ-19 · `b`,`d` phủ REQ-20 · `c`,`d` chứng minh hành vi giống nhau giữa hai vai trò<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): `body` mang class `user-id-2` ở `a`,`b` và `user-id-3` ở `c`,`d` | Medium | Yes | UI | @Regression, @Permission, @TechCheck, @Login |
| CRM_LOGIN_TC_024 | REQ-LOGIN-21 | LOGIN | High | Biểu mẫu đăng nhập mang mã chống CSRF đúng hình thái và gửi kèm khi submit | Đang ở `/admin/authentication`, trang vừa nạp | 1. Mở `/admin/authentication`<br>2. Nhấn F5 nạp lại trang<br>3. Nhập Email `admin@example.com` + Password `ADMIN_PASSWORD` trong `.env`, tích `Remember me`<br>4. Bấm `Login`<br>5. Thực hiện phần 🔧 nếu có người chạy được | Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env`<br>Remember me: **tích** | 4. Đăng nhập thành công, dừng ở `https://crm.anhtester.com/admin/`<br><br>⚠️ **Mã chống CSRF không có biểu hiện nhìn thấy được.** Phần chính chỉ chứng minh biểu mẫu gửi được bình thường; kiểm chứng thật nằm ở dòng 🔧. Cặp phủ định của TC này là `CRM_LOGIN_TC_025` (mã sai thì bị chặn)<br><br>🔧 Ghi chú kỹ thuật (cần DevTools):<br>`1` Trang có đúng **1** `input[type=hidden][name=csrf_token_name]`; `value` không rỗng, dài đúng **32** ký tự, toàn bộ thuộc tập hex `0-9a-f`. 🔒 Ghi hình thái, KHÔNG chép giá trị token<br>`2` Sau F5, `value` **khác** lần trước — mã sinh lại ở mỗi lần nạp trang. ⚠️ **Chưa có evidence**; nếu hai giá trị giống nhau thì ghi nhận "mã cố định theo phiên" và mở ambiguity mới, KHÔNG kết luận là lỗi<br>`3` Thân `POST /admin/authentication` chứa đủ 4 tham số `csrf_token_name`, `email`, `password`, `remember`; giá trị `remember` là chuỗi `estimate`. ⚠️ Automation **CẤM** dùng `value="estimate"` làm điều kiện thao tác ô tích (`AMB-11`) — chỉ thao tác theo trạng thái tích/không tích | High | Partial | UI | @Regression, @Security, @NeedsVerify, @TechCheck, @Login |
| CRM_LOGIN_TC_025 | REQ-LOGIN-22 | LOGIN | High | Gửi biểu mẫu với mã CSRF không hợp lệ thì bị chặn, không xử lý đăng nhập | Đang ở `/admin/authentication`. **TC này bắt buộc dùng DevTools để sửa mã ẩn** — không có cách nào thao tác bằng giao diện. Nạp lại trang trước **mỗi** biến thể | 1. Mở DevTools, sửa `value` của `input[name=csrf_token_name]` theo biến thể<br>2. Nhập Email `admin@example.com` + Password `ADMIN_PASSWORD` trong `.env`<br>3. Bấm nút `Login`<br>4. Đọc chữ trên tab trình duyệt<br>5. Đọc nội dung hiển thị trên trang<br>6. Kiểm có vào được Dashboard không | **Bảng biến thể:**<br>`a` Mã bị sửa → `00000000000000000000000000000000`<br>`b` Mã bị xoá rỗng → *(chuỗi rỗng)*<br><br>Email: `admin@example.com` · Password: giá trị `ADMIN_PASSWORD` trong `.env` | **Mọi biến thể cho cùng kết quả:**<br>3. Trang chuyển sang trang lỗi<br>4. Tab trình duyệt ghi `Error`<br>5. Màn hình hiển thị nguyên văn 2 dòng: `419 Page Expired!` và `Sorry, the page has expired, return to previous page and refresh to continue.`<br>6. **Không** vào được `/admin/`, không đăng nhập được<br><br>⚠️ Chấm theo **chữ hiển thị trên màn hình**, KHÔNG chấm theo mã HTTP (`AMB-08`). Biến thể `b` **chưa có evidence** — evidence chỉ có trường hợp mã bị sửa | Critical | Yes | UI | @Regression, @Security, @NeedsVerify, @TechCheck, @Login |
| CRM_LOGIN_TC_026 | REQ-LOGIN-42 | LOGIN | High | Phiên đăng nhập hết hạn sau 1 giờ không thao tác | Đã đăng nhập bằng Admin, đang ở `/admin/`. **TC chạy mất 65 phút** — không xếp vào bộ smoke | 1. Đăng nhập bằng `admin@example.com` + `ADMIN_PASSWORD` trong `.env`, xác nhận đang ở Dashboard<br>2. Ghi lại thời điểm đăng nhập<br>3. Để yên trình duyệt **65 phút**, KHÔNG thao tác gì, không nạp lại trang<br>4. Sau 65 phút, gõ thẳng `https://crm.anhtester.com/admin/clients`<br>5. Đọc URL và quan sát nội dung trang | Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env`<br>Thời gian chờ: **65 phút** (mốc 1 giờ + 5 phút dự phòng) | 1. Đăng nhập thành công, vào Dashboard<br>4. Trang tự chuyển hướng<br>5. Dừng ở `https://crm.anhtester.com/admin/authentication` với biểu mẫu đăng nhập — giống hệt trường hợp chưa đăng nhập, không thấy dữ liệu khách hàng nào<br><br>⚠️ **Dựa trên giả định** phiên tính theo **thời gian không hoạt động** (`AMB-19` còn treo 🟡). Nếu phiên tính theo tổng thời gian từ lúc đăng nhập thì TC này vẫn PASS nhưng **không** phân biệt được hai cách — cần TC bổ sung khi `AMB-19` được trả lời | High | Partial | UI | @Regression, @Slow, @AssumptionBased, @Login |

---

## Nhóm E — Quên mật khẩu & Đặt lại mật khẩu

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automatable | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_027 | REQ-LOGIN-23, REQ-LOGIN-24 | LOGIN | Medium | Trang Quên mật khẩu mở đúng, đủ thành phần và không có lối quay lại đăng nhập | Chưa đăng nhập, đang ở `/admin/authentication` | 1. Bấm liên kết `Forgot Password?` dưới nút Login<br>2. Nhìn khắp trang, đối chiếu lần lượt từng mục trong Bảng kiểm | — (chỉ điều hướng + quan sát) | **Bảng kiểm — mọi mục phải đạt:**<br>`1` (REQ-23) Sau khi bấm, thanh địa chỉ dừng ở `https://crm.anhtester.com/admin/authentication/forgot_password`<br>`2` (REQ-23) Khối biểu mẫu có tiêu đề `Forgot Password`, đúng **1** ô nhập nhãn `Email Address`, và nút `Confirm` nền xanh chiếm hết chiều ngang khối<br>`3` (REQ-24) Trên trang **không** có liên kết nào dẫn ngược về trang đăng nhập — dưới nút `Confirm` **không** có dòng "Back to login"; thứ duy nhất bấm được ngoài biểu mẫu là logo, dẫn về `https://crm.anhtester.com/`<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): `<form>` có `action` = `https://crm.anhtester.com/admin/authentication/forgot_password`, `method` = `post`; tồn tại đúng **1** `input[type=hidden][name=csrf_token_name]` dài **32** ký tự hex; toàn trang đúng **1** thẻ `<a>` | High | Yes | UI | @Regression, @Security, @TechCheck, @Login |
| CRM_LOGIN_TC_028 | REQ-LOGIN-25 | LOGIN | High | 🐞 Bỏ trống email rồi bấm Confirm phải báo thiếu trường bắt buộc | Đang ở `/admin/authentication/forgot_password`, ô email rỗng | 1. Xác nhận ô `Email Address` đang rỗng<br>2. Bấm nút `Confirm`<br>3. Đếm số dải báo lỗi đỏ<br>4. Đọc nội dung dải<br>5. Đọc URL trên thanh địa chỉ | Email: (để trống) | 2. Trang nạp lại<br>3. Đúng **1** dải<br>4. Nội dung **phải** là thông báo trường bắt buộc, nhất quán với trang đăng nhập: `The Email Address field is required.`<br>5. Vẫn ở `https://crm.anhtester.com/admin/authentication/forgot_password`<br><br>🐞 **Hiện trạng: FAIL** — hệ thống trả `Email not found`, báo sai bản chất lỗi và làm người dùng tưởng email của mình không tồn tại. PO xác nhận là **thiếu validate trường bắt buộc** (`AMB-05`). FAIL là kết quả **đúng** → mở bug, **KHÔNG** sửa TC theo hiện trạng | High | Yes | UI | @Regression, @KnownBug, @Login |
| CRM_LOGIN_TC_029 | REQ-LOGIN-26 | LOGIN | High | Nhập email không tồn tại trong hệ thống thì báo không tìm thấy | Đang ở `/admin/authentication/forgot_password` | 1. Nhập ô Email: `notexist_20260820@auto.test`<br>2. Bấm nút `Confirm`<br>3. Đếm số dải báo lỗi đỏ<br>4. Đọc nội dung dải<br>5. Đọc URL và nhìn ô Email | Email: `notexist_20260820@auto.test` | 2. Trang nạp lại<br>3. Đúng **1** dải<br>4. Nội dung nguyên văn = `Email not found`<br>5. Vẫn ở `https://crm.anhtester.com/admin/authentication/forgot_password`, ô Email trở về rỗng | High | Yes | UI | @Regression, @Login |
| CRM_LOGIN_TC_030 | REQ-LOGIN-26 | LOGIN | Medium | Email sai định dạng ở trang Quên mật khẩu bị trình duyệt chặn | Đang ở `/admin/authentication/forgot_password` | 1. Nhập ô Email: `abc`<br>2. Bấm nút `Confirm`<br>3. Quan sát khu vực ô Email<br>4. Quan sát trang có nạp lại không | Email: `abc` | 3. Trình duyệt bật bong bóng cảnh báo ngay cạnh ô Email — nội dung bong bóng do trình duyệt sinh, **không** dùng làm điều kiện chấm<br>4. Trang **không** nạp lại, chữ `abc` còn nguyên trong ô, **không** hiện dải báo lỗi của hệ thống<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): tab Network **không** ghi nhận request `POST /admin/authentication/forgot_password` nào | Medium | Yes | UI | @Regression, @Boundary, @TechCheck, @Login |
| CRM_LOGIN_TC_031 | REQ-LOGIN-28 | LOGIN | Medium | Mở liên kết đặt lại mật khẩu không hợp lệ thì máy chủ trả lỗi 500 với thân rỗng | Chưa đăng nhập.<br>📌 `Auto Type` = `API` — TC này kiểm ở tầng giao thức, được phép dùng ngôn ngữ kỹ thuật ở phần chính | 1. Gõ thẳng URL của biến thể vào thanh địa chỉ<br>2. Đọc mã trạng thái HTTP của phản hồi<br>3. Đọc độ dài thân phản hồi<br>4. Quan sát nội dung hiển thị trên màn hình | **Bảng biến thể** (mã khoá sai dùng chung: 40 ký tự `0`):<br>`a` Mã khoá sai → `/admin/authentication/reset_password/2/0000000000000000000000000000000000000000`<br>`b` Mã người dùng không tồn tại → `/admin/authentication/reset_password/999999/0000000000000000000000000000000000000000` | **Mọi biến thể cho cùng kết quả:**<br>2. HTTP `500`<br>3. Thân phản hồi **rỗng**<br>4. Trình duyệt hiển thị trang lỗi mặc định của chính nó, **không** có trang thông báo thân thiện của ứng dụng<br><br>✅ Đây là **hành vi được chấp nhận** theo quyết định 2026-08-18 (`AMB-03` ⏭️) — TC ghi nhận đúng hiện trạng, **KHÔNG** kỳ vọng trang thông báo thân thiện.<br>⚠️ Biến thể `b` **chưa có evidence** | Medium | Yes | API | @Regression, @NeedsVerify, @Login |

---

## Nhóm F — Đăng xuất & vòng đời cookie ghi nhớ

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automatable | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_032 | REQ-LOGIN-29 | LOGIN | High | Ở viewport desktop chỉ có một lối đăng xuất dùng được — trong menu ảnh đại diện | Đã đăng nhập bằng Admin, đang ở `/admin/`, viewport `1600×750` | 1. Nhìn khắp thanh đầu trang tìm nút hoặc chữ `Logout`<br>2. Bấm vào ảnh đại diện tròn góc trên bên phải<br>3. Đọc danh sách mục trong menu vừa mở, theo thứ tự trên xuống<br>4. Bấm vào mục `Logout` | — (chỉ quan sát + tương tác) | 1. Thanh đầu trang **không** có nút Logout nào nhìn thấy được — lối duy nhất nằm trong menu ảnh đại diện<br>2. Menu mở ra ngay dưới ảnh đại diện<br>3. Menu có 5 mục theo thứ tự `My Profile` → `My Timesheets` → `Edit Profile` → `Language ▸` → `Logout`; `Logout` là mục **cuối cùng**<br>4. Bấm được, trình duyệt bắt đầu chuyển hướng đăng xuất<br><br>🔧 Ghi chú kỹ thuật (cần DevTools + dành cho automation): trong DOM có **2** phần tử `li.header-logout`; phần tử thuộc `ul.nav.navbar-nav` có hộp `0×0`, `offsetParent` = `null`, hai tổ tiên `div.mobile-navbar.collapse` và `div.mobile-menu` đều `display: none`. Phần tử desktop dùng được có hộp `160×64`.<br>⚠️ Locator automation phải là `.dropdown-menu > li.header-logout` (**con trực tiếp**) — menu có 32 `<li>` lồng trong submenu Language nên `.dropdown-menu li:last-child` trỏ nhầm sang mục ngôn ngữ | Critical | Yes | UI | @Smoke, @CriticalPath, @TechCheck, @Login |
| CRM_LOGIN_TC_033 | REQ-LOGIN-29 | LOGIN | Medium | Ở viewport mobile đăng xuất được qua menu điều hướng thu gọn | Đã đăng nhập bằng Admin, chuyển trình duyệt sang viewport mobile rồi **nạp lại trang** | 1. Đặt cửa sổ về kích thước mobile `375×812` và nạp lại `/admin/`<br>2. Bấm nút mở menu điều hướng thu gọn (biểu tượng ba gạch)<br>3. Tìm mục `Logout` trong menu vừa mở<br>4. Bấm vào `Logout`<br>5. Đọc URL sau khi trang dừng | Viewport: `375×812` | 2. Menu thu gọn mở ra<br>3. Nhìn thấy mục `Logout` — hiển thị rõ, bấm được<br>5. Dừng ở `https://crm.anhtester.com/admin/authentication`<br><br>⚠️ **Chưa có evidence và chưa recon ở viewport mobile.** Nguồn duy nhất là quyết định PO (`AMB-16`) — phải recon mobile trước khi coi TC này là đáng tin | Medium | Partial | UI | @Regression, @NeedsVerify, @Login |
| CRM_LOGIN_TC_034 | REQ-LOGIN-30 | LOGIN | Medium | Đăng xuất trong lúc còn bộ đếm giờ công việc đang chạy thì phải xác nhận trước | Đã đăng nhập, đang ở `/admin/` và **có ít nhất 1 bộ đếm giờ công việc đang chạy** (nhìn thấy trên thanh đầu trang) | 1. Xác nhận trên thanh đầu trang đang hiện ít nhất 1 bộ đếm giờ chạy<br>2. Bấm ảnh đại diện, bấm `Logout`<br>3. Quan sát có hộp thoại xác nhận bật lên không<br>4. Đọc nội dung hộp thoại<br>5. Đọc URL xem đã rời trang hiện tại chưa | Điều kiện dữ liệu: ít nhất 1 task có bộ đếm giờ đang chạy | 1. Thanh đầu trang hiện bộ đếm giờ đang chạy<br>3. Hộp thoại xác nhận bật lên giữa màn hình<br>4. Nội dung nguyên văn `Started tasks timers found!` và `Are you sure you want to logout without stopping the timers?`, kèm nút `Logout`<br>5. **Chưa** đăng xuất — vẫn ở trang hiện tại cho tới khi người dùng chọn<br><br>⚠️ **Bằng chứng hiện dừng ở mức đọc mã nguồn hàm `logout()`**, chưa chạy thật đầu-cuối. Việc kiểm chứng thuộc module `TASK` (`AMB-14` ⏭️) — chạy xong ở `TASK` phải cập nhật ngược TC này | Medium | No | N/A | @Regression, @AssumptionBased, @NeedsVerify, @Login |
| CRM_LOGIN_TC_035 | REQ-LOGIN-31 | LOGIN | High | Không có bộ đếm giờ nào chạy thì bấm Logout đi thẳng, không hỏi lại | Đã đăng nhập bằng Admin, đang ở `/admin/`, **không** có bộ đếm giờ công việc nào đang chạy | 1. Xác nhận thanh đầu trang **không** hiện bộ đếm giờ nào<br>2. Bấm ảnh đại diện, bấm `Logout`<br>3. Quan sát có hộp thoại xác nhận nào bật lên không<br>4. Đọc URL sau khi trang dừng | — (không cần dữ liệu đầu vào) | 1. Không bộ đếm giờ nào hiển thị<br>3. **Không** hộp thoại xác nhận nào bật lên<br>4. Đi thẳng và dừng ở `https://crm.anhtester.com/admin/authentication` với biểu mẫu đăng nhập | High | Yes | UI | @Smoke, @Regression, @Login |
| CRM_LOGIN_TC_036 | REQ-LOGIN-32, REQ-LOGIN-33 | LOGIN | High | Đăng xuất kết thúc phiên và URL nội bộ không dùng lại được, kể cả bằng nút Back | Theo từng biến thể — xem cột Test Data | 1. Đưa trình duyệt về trạng thái tiền đề của biến thể<br>2. Mở `https://crm.anhtester.com/admin/authentication/logout`<br>3. Đọc URL, quan sát trang và đếm dải thông báo bất kỳ (đỏ hay xanh)<br>4. *(chỉ biến thể `a`)* Gõ thẳng `https://crm.anhtester.com/admin/clients`, đọc URL và quan sát nội dung<br>5. *(chỉ biến thể `a`)* Bấm nút Back của trình duyệt, quan sát nội dung hiển thị | **Bảng biến thể:**<br>`a` ⭐smoke Đang có phiên → đăng nhập `admin@example.com` + `ADMIN_PASSWORD` trong `.env`, **mở `/admin/clients` một lần** cho trang vào lịch sử, rồi mới chạy bước 2<br>`b` Chưa đăng nhập → trình duyệt sạch, chưa đăng nhập | **Mọi biến thể (bước 3):** dừng ở `https://crm.anhtester.com/admin/authentication`; hiển thị biểu mẫu đăng nhập với hai ô rỗng, **không** trang lỗi; **không** dải thông báo nào — kể cả thông báo đăng xuất thành công<br><br>**Riêng biến thể `a`:**<br>4. Dừng ở `/admin/authentication`, **không** thấy dữ liệu khách hàng nào<br>5. Bấm Back **không** hiển thị lại dữ liệu đã tải trước đó, bị đưa về `/admin/authentication` (REQ-33)<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): phản hồi `/admin/clients` mang header `cache-control: no-store, no-cache, must-revalidate` và `pragma: no-cache`; cookie `autologin` có thể vẫn còn trong trình duyệt nhưng không giúp truy cập được — phiên đã mất hiệu lực phía máy chủ | Critical | Yes | UI | @Smoke, @CriticalPath, @Security, @TechCheck, @Login |
| CRM_LOGIN_TC_037 | REQ-LOGIN-39 | LOGIN | High | Cookie ghi nhớ còn lại sau khi đăng xuất chủ động không tự đăng nhập lại | Trạng thái sạch: đã đăng xuất và đã xoá cookie của `crm.anhtester.com` | 1. Đăng nhập bằng `admin@example.com` + `ADMIN_PASSWORD` trong `.env`, **có tích** `Remember me`<br>2. Xác nhận đã vào Dashboard<br>3. Mở `https://crm.anhtester.com/admin/authentication/logout`<br>4. Gõ thẳng `https://crm.anhtester.com/admin/`<br>5. Đọc URL và quan sát nội dung trang | Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env`<br>Remember me: **tích** | 2. Vào được Dashboard<br>3. Đăng xuất thành công, dừng ở `/admin/authentication`<br>5. Dừng ở `https://crm.anhtester.com/admin/authentication` với biểu mẫu đăng nhập — hệ thống **không** tự đăng nhập lại<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): sau bước 2 tồn tại cookie `autologin` khớp hình thái mô tả ở `CRM_LOGIN_TC_008`; sau bước 3 cookie đó **vẫn còn** nguyên giá trị — đây chính là điểm TC muốn chứng minh: cookie còn nhưng vô hiệu | High | Partial | UI | @Regression, @Security, @TechCheck, @Login |

---

## Nhóm G — Phi chức năng

| TC ID | REQ ID | Module | Risk Level | Test Scenario | Pre-Condition | Test Steps | Test Data | Expected Result | Priority | Automatable | Auto Type | Tags |
|---|---|---|---|---|---|---|---|---|---|---|---|---|
| CRM_LOGIN_TC_038 | REQ-LOGIN-02 | LOGIN | Medium | Điều hướng và gửi biểu mẫu đăng nhập hoàn toàn bằng bàn phím, có viền tiêu điểm rõ ràng | Đang ở `/admin/authentication`, trang vừa nạp, chưa bấm chuột vào đâu | 1. Quan sát viền ô Email so với viền ô Password<br>2. Gõ `admin@example.com` — không bấm chuột<br>3. Nhấn `Tab`, quan sát viền hai ô<br>4. Gõ giá trị `ADMIN_PASSWORD` trong `.env`<br>5. Nhấn `Tab`, quan sát ô tích `Remember me`<br>6. Nhấn phím `Space`, quan sát ô tích<br>7. Nhấn `Tab` rồi nhấn `Enter` | Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env` | 1. Ô Email có viền xanh dương đậm dày hơn, ô Password viền xám nhạt mỏng — khác nhau rõ ràng bằng mắt<br>2. Chữ gõ vào rơi đúng ô Email, không cần bấm chuột<br>3. Viền xanh chuyển sang ô Password, ô Email trở về viền xám nhạt<br>4. Chữ gõ vào rơi đúng ô Password, hiện dấu chấm che<br>5. Viền tiêu điểm chuyển tới ô tích `Remember me`<br>6. Ô tích chuyển từ rỗng sang tích ✔ — phím `Space` tích được<br>7. Viền tiêu điểm chuyển tới nút `Login`; `Enter` gửi biểu mẫu và đăng nhập thành công vào `https://crm.anhtester.com/admin/` | Medium | Yes | UI | @Regression, @Accessibility, @Login |
| CRM_LOGIN_TC_039 | REQ-LOGIN-01 | LOGIN | High | 🐞 Truy cập trang đăng nhập qua HTTP thuần phải bị ép chuyển hướng sang HTTPS | Trình duyệt sạch, chưa đăng nhập | 1. Gõ thẳng `http://crm.anhtester.com/admin/authentication` (giao thức **http**, gõ đủ chữ `http://`)<br>2. Đợi trang tải xong<br>3. Đọc phần đầu URL trên thanh địa chỉ<br>4. Nhìn biểu tượng bên trái thanh địa chỉ<br>5. Thực hiện phần 🔧 nếu có người chạy được | URL: `http://crm.anhtester.com/admin/authentication` | 3. Thanh địa chỉ **phải** đã chuyển thành `https://crm.anhtester.com/admin/authentication` — trình duyệt tự đổi giao thức<br>4. Hiện biểu tượng ổ khoá / kết nối an toàn, **không** có cảnh báo `Not secure`<br><br>🐞 **Nhiều khả năng FAIL.** Một bug report cũ trong repo ([BUG_login_1785678750_TC039.md](../../bugs/login/BUG_login_1785678750_TC039.md), 2026-08-02) đo được trên **chính hệ thống này**: không chuyển hướng, thanh địa chỉ giữ nguyên `http://`. Bug report đó thuộc **đợt kiểm thử trước**, TC ID và REQ ID trong đó **không khớp** bộ tài liệu hiện tại — cần đo lại rồi cập nhật hoặc mở bug mới<br>⚠️ Requirements mục 9 chỉ ghi "toàn bộ qua HTTPS", **không** khẳng định có chuyển hướng ép buộc — kỳ vọng ở TC này là **chuẩn bảo mật**, chưa được xác nhận là yêu cầu đã chốt<br><br>🔧 Ghi chú kỹ thuật (cần DevTools): request HTTP đầu tiên phải trả `301`/`308` kèm header `Location: https://crm.anhtester.com/admin/authentication`; bản HTTPS phải mang header `Strict-Transport-Security`. Hiện trạng đo được ở bug cũ: HTTP `200`, không header `Location`, không HSTS | High | Yes | UI | @Regression, @Security, @KnownBug, @NeedsVerify, @TechCheck, @Login |
| CRM_LOGIN_TC_040 | REQ-LOGIN-14 | LOGIN | Medium | Mất kết nối mạng giữa lúc gửi biểu mẫu thì không tạo phiên | Đang ở `/admin/authentication`, trang đã tải xong | 1. Nhập Email `admin@example.com` + Password `ADMIN_PASSWORD` trong `.env`<br>2. Ngắt kết nối mạng của máy (tắt Wi-Fi hoặc rút cáp)<br>3. Bấm nút `Login`<br>4. Quan sát nội dung hiển thị<br>5. Bật lại mạng, mở `https://crm.anhtester.com/admin/`<br>6. Đọc URL và quan sát nội dung trang | Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env`<br>Trạng thái mạng: **ngắt kết nối** | 3. Trang không gửi đi được<br>4. Trình duyệt hiển thị trang lỗi kết nối của chính nó; ứng dụng **không** hiện dải thông báo sai lệch nào (không báo "đăng nhập thành công", không báo sai mật khẩu)<br>6. Dừng ở `https://crm.anhtester.com/admin/authentication` với biểu mẫu đăng nhập — **không** phiên nào được tạo trong lúc mất mạng<br><br>⚠️ **Chưa có evidence** cho tình huống này | Medium | Partial | UI | @Regression, @NetworkResilience, @NeedsVerify, @Login |
| CRM_LOGIN_TC_041 | REQ-LOGIN-06 | LOGIN | Low | Đăng nhập trên đường truyền chậm vẫn hoàn tất và vào đúng Dashboard | Đang ở `/admin/authentication`. **Cần DevTools để giả lập mạng chậm** — không có cách nào làm bằng giao diện thường | 1. Mở DevTools → tab Network, đặt chế độ mạng thành `Slow 3G`<br>2. Nhập Email `admin@example.com` + Password `ADMIN_PASSWORD` trong `.env`<br>3. Bấm nút `Login` **một lần duy nhất**<br>4. Đợi trang xử lý xong, không bấm thêm gì<br>5. Đọc URL và chữ trên tab trình duyệt | Email: `admin@example.com`<br>Password: giá trị `ADMIN_PASSWORD` trong `.env`<br>Chế độ mạng: `Slow 3G` | 3. Trang bắt đầu xử lý, **không** báo lỗi ngay<br>4. **Không** xuất hiện thông báo hết thời gian chờ, trang không đứng hẳn<br>5. Dừng ở `https://crm.anhtester.com/admin/`, tab trình duyệt có chữ `Dashboard`<br><br>⚠️ **Chưa có evidence** cho tình huống mạng chậm | Low | Partial | UI | @Regression, @NetworkResilience, @NeedsVerify, @TechCheck, @Login |

---

## 🐞 Ba TC nhiều khả năng FAIL — mở bug, KHÔNG sửa TC

| TC ID | REQ | Vì sao FAIL là kết quả đúng | Trạng thái |
|---|---|---|---|
| `CRM_LOGIN_TC_016` | REQ-LOGIN-16 | Sau khi đăng nhập lỗi, ô Email **phải** còn email vừa nhập. Hiện trạng (chạy ở cửa sổ ẩn danh, tắt tự điền): ô trở về rỗng. PO xác nhận là **lỗi hệ thống** (`AMB-10` ✅) | Chắc chắn FAIL — đã đo |
| `CRM_LOGIN_TC_028` | REQ-LOGIN-25 | Bỏ trống email ở Quên mật khẩu **phải** báo trường bắt buộc. Hiện trạng trả `Email not found`. PO xác nhận là **lỗi** (`AMB-05` ✅) | Chắc chắn FAIL — đã đo |
| `CRM_LOGIN_TC_039` | REQ-LOGIN-01 | HTTP thuần **phải** bị ép chuyển sang HTTPS. Bug report cũ đo được: không chuyển hướng, không HSTS | ⚠️ Chưa xác nhận lại — bug report thuộc đợt kiểm thử trước, cần đo lại |

---

## Assumptions đã áp dụng

| Mã | Điểm chưa rõ | Giả định đã dùng | TC / biến thể bị ảnh hưởng |
|---|---|---|---|
| ASM-01 | `AMB-19` còn treo 🟡 — phiên 1 giờ tính theo **thời gian không hoạt động** hay **tổng thời gian từ lúc đăng nhập**? | Tạm hiểu là **thời gian không hoạt động** — TC để yên 65 phút rồi kiểm | `TC_026` — `@AssumptionBased` |
| ASM-02 | `REQ-LOGIN-30` mới có bằng chứng đọc mã nguồn `logout()`, chưa chạy thật đầu-cuối (`AMB-14` ⏭️ chuyển module `TASK`) | Viết TC theo đúng mã nguồn đã đọc, chấp nhận bằng chứng mức mã nguồn | `TC_034` — `@AssumptionBased` + `@NeedsVerify`, `Automatable = No` |
| ASM-03 | `REQ-LOGIN-29` nhánh mobile chưa có lượt recon nào — nguồn duy nhất là quyết định PO (`AMB-16`) | Viết TC theo quyết định PO; kỳ vọng ở TC_033 **chưa được đo** | `TC_033` — `@NeedsVerify` |
| ASM-04 | Hệ thống xử lý chuỗi toàn khoảng trắng ở trường bắt buộc thế nào? | Giả định **cắt khoảng trắng trước khi kiểm trường bắt buộc**, nhất quán với `REQ-LOGIN-38` | `TC_011-d` — `@NeedsVerify` |
| ASM-05 | `REQ-LOGIN-21` ghi mã CSRF "đổi theo phiên" — không rõ có sinh lại ở mỗi lần nạp trang không | Giả định **sinh lại ở mỗi lần nạp trang**; thực tế giữ nguyên thì ghi nhận hiện trạng, không kết luận là lỗi | `TC_024` mục 🔧`2` — `@NeedsVerify` |
| ASM-06 | Mật khẩu tài khoản demo là **6 ký tự số** (`RISK-07`) nên không có chữ cái để thử đảo hoa/thường | TC chỉ chạy được khi tài khoản test có mật khẩu chứa chữ cái | `TC_014` — `@NeedsVerify` |
| ASM-07 | Requirements mục 9 ghi "toàn bộ qua HTTPS" nhưng **không** khẳng định có chuyển hướng ép buộc từ HTTP | Viết TC theo **chuẩn bảo mật** (phải chuyển sang HTTPS + có HSTS), đối chiếu với bug report cũ | `TC_039` — `@KnownBug` + `@NeedsVerify` |
| ASM-08 | Mã CSRF gửi rỗng có bị chặn như mã bị sửa không (chưa có evidence) | Giả định **bị chặn giống nhau** — cùng trang lỗi `419 Page Expired!` | `TC_025-b` — `@NeedsVerify` |
| ASM-09 | `REQ-LOGIN-08` (phát hành cookie ghi nhớ) không có hệ quả nghiệp vụ nào kiểm được bằng mắt, vì `REQ-LOGIN-40` ghi nhận tính năng tự đăng nhập lại **không hoạt động** (`AMB-15` ⏭️) | Phần chính của `TC_008`/`TC_009` chỉ chấm tới bước đăng nhập thành công; kiểm chứng cookie đặt ở dòng 🔧 và TC gắn `@TechCheck` | `TC_008`, `TC_009` — `@TechCheck` |

> ✅ **Không phát hiện xung đột nào giữa tài liệu và evidence.** Cả 9 ảnh đều xác nhận đúng mô tả trong `requirements_login.md`.

---

## Bảng Đối Soát Coverage (39/39 REQ trong phạm vi)

| REQ ID | Mô tả ngắn | Số TC | TC IDs (kèm biến thể) | Đủ Positive/Negative/Boundary? |
|---|---|---|---|---|
| REQ-LOGIN-01 | Truy cập trang đăng nhập | 2 | TC_001, TC_039 | ✅ |
| REQ-LOGIN-02 | Thành phần biểu mẫu đăng nhập | 2 | TC_002 (6 mục), TC_038 | ✅ |
| REQ-LOGIN-03 | Tự đặt con trỏ vào ô Email | 1 | TC_002-`2` | ✅ |
| REQ-LOGIN-04 | Logo dẫn về trang chủ | 1 | TC_004 | ✅ |
| REQ-LOGIN-05 | Không có CAPTCHA | 1 | TC_003-`1` | ✅ (kiểm sự vắng mặt) |
| REQ-LOGIN-06 | Đăng nhập bằng thông tin hợp lệ | 4 | TC_005, TC_006, TC_010, TC_041 | ✅ |
| REQ-LOGIN-07 | Email không phân biệt hoa thường | 1 | TC_007-`a`,`b` | ✅ |
| REQ-LOGIN-08 | Ghi nhớ đăng nhập sinh cookie ghi nhớ | 1 | TC_008 | ✅ (cặp với TC_009) |
| REQ-LOGIN-09 | Không tích thì không phát hành cookie | 1 | TC_009 | ✅ (nhánh phủ định của REQ-08) |
| REQ-LOGIN-10 | Bỏ trống cả hai trường | 1 | TC_011-`a`,`d` | ✅ |
| REQ-LOGIN-11 | Bỏ trống riêng Email | 1 | TC_011-`b` | ✅ |
| REQ-LOGIN-12 | Bỏ trống riêng Mật khẩu | 1 | TC_011-`c` | ✅ |
| REQ-LOGIN-13 | Chặn email sai định dạng tại trình duyệt | 1 | TC_012-`a`→`e` | ✅ |
| REQ-LOGIN-14 | Thông báo khi sai thông tin đăng nhập | 5 | TC_013, TC_014, TC_017, TC_018, TC_040 | ✅ |
| REQ-LOGIN-15 | Thông báo không tiết lộ email nào có thật | 2 | TC_013 (bước 5), TC_019 (bước 4) | ✅ |
| REQ-LOGIN-16 | 🐞 Ô Email phải giữ lại email sau lỗi | 1 | TC_016 | ✅ (TC sẽ FAIL — đúng thiết kế) |
| REQ-LOGIN-17 | Chặn URL nội bộ khi chưa đăng nhập | 1 | TC_021-`a`,`b` | ✅ |
| REQ-LOGIN-18 | Không ghi nhớ URL đích sau chuyển hướng | 1 | TC_022 | ✅ |
| REQ-LOGIN-19 | Đã đăng nhập không vào lại trang đăng nhập | 1 | TC_023-`a`,`c` | ✅ (2 vai trò) |
| REQ-LOGIN-20 | Đã đăng nhập không vào trang Quên mật khẩu | 1 | TC_023-`b`,`d` | ✅ (2 vai trò) |
| REQ-LOGIN-21 | Biểu mẫu mang mã chống CSRF | 1 | TC_024 (🔧 3 mục) | ✅ |
| REQ-LOGIN-22 | Từ chối yêu cầu có mã CSRF sai | 1 | TC_025-`a`,`b` | ✅ |
| REQ-LOGIN-23 | Truy cập trang Quên mật khẩu | 1 | TC_027-`1`,`2` | ✅ |
| REQ-LOGIN-24 | Không có lối quay lại đăng nhập | 1 | TC_027-`3` | ✅ (kiểm sự vắng mặt) |
| REQ-LOGIN-25 | 🐞 Bỏ trống email phải báo trường bắt buộc | 1 | TC_028 | ✅ (TC sẽ FAIL — đúng thiết kế) |
| REQ-LOGIN-26 | Email không tồn tại ở Quên mật khẩu | 2 | TC_029, TC_030 | ✅ |
| REQ-LOGIN-28 | Liên kết đặt lại mật khẩu sai/hết hạn | 1 | TC_031-`a`,`b` | ✅ |
| REQ-LOGIN-29 | Mỗi viewport có một lối đăng xuất dùng được | 2 | TC_032, TC_033 | ✅ |
| REQ-LOGIN-30 | Cảnh báo khi còn bộ đếm giờ đang chạy | 1 | TC_034 | ✅ (giới hạn bởi `AMB-14`) |
| REQ-LOGIN-31 | Đăng xuất ngay khi không có bộ đếm giờ | 1 | TC_035 | ✅ (nhánh phủ định của REQ-30) |
| REQ-LOGIN-32 | Kết thúc phiên và về trang đăng nhập | 1 | TC_036-`a`,`b` | ✅ |
| REQ-LOGIN-33 | URL nội bộ bị chặn sau khi đăng xuất | 1 | TC_036 bước 4–5 | ✅ |
| REQ-LOGIN-36 | Không có đăng nhập mạng xã hội | 1 | TC_003-`2` | ✅ (kiểm sự vắng mặt) |
| REQ-LOGIN-37 | Trang không nạp tệp JavaScript nào | 1 | TC_003-`3` | ✅ (kiểm sự vắng mặt) |
| REQ-LOGIN-38 | Email bỏ qua khoảng trắng thừa | 1 | TC_007-`c`,`d` | ✅ |
| REQ-LOGIN-39 | Cookie không tự đăng nhập lại sau đăng xuất | 1 | TC_037 | ✅ |
| REQ-LOGIN-41 | Không khoá tài khoản sau nhiều lần sai | 1 | TC_015-`a`,`b` | ✅ |
| REQ-LOGIN-42 | Phiên hết hạn sau 1 giờ | 1 | TC_026 | ✅ (giới hạn bởi `AMB-19`) |
| REQ-LOGIN-43 | Customer không đăng nhập được vào `/admin` | 2 | TC_019, TC_020 | ✅ |

**Kết luận:** 39/39 REQ trong phạm vi có ≥1 TC. **Không có dòng 🔴.** Tổng số **case** kiểm không đổi so với bản trước lần cập nhật ngôn ngữ — chỉ đổi cách phát biểu kỳ vọng.

### REQ cố ý KHÔNG có TC (ngoài phạm vi theo quyết định PO 2026-08-18)

| REQ ID | Lý do | Ambiguity |
|---|---|---|
| REQ-LOGIN-27 | Không kiểm chứng luồng gửi mail đặt lại với email có thật | `AMB-04` ⏭️ |
| REQ-LOGIN-34 | Cookie ghi nhớ không bị xoá khi đăng xuất — chỉ ghi nhận hiện trạng | `AMB-06` ⏭️ |
| REQ-LOGIN-35 | Cookie ghi nhớ thiếu cờ `HttpOnly` — chỉ ghi nhận hiện trạng cho `RISK-02` | `AMB-07` ⏭️ |
| REQ-LOGIN-40 | Ghi nhớ đăng nhập khi phiên hết hạn tự nhiên — PO xác nhận tính năng **không hoạt động** | `AMB-15` ⏭️ |

---

## Bảng Đối Soát Evidence

| Ảnh evidence | Màn hình / trạng thái | TC dựa vào | Đầy đủ? |
|---|---|---|---|
| `login_form_default_fullpage.png` | Đăng nhập — mặc định, hai ô rỗng | TC_001, TC_002, TC_003, TC_004, TC_027, TC_038 | ✅ full-page, thấy trọn khối biểu mẫu tới liên kết `Forgot Password?` |
| `login_form_filled_remember_checked_fullpage.png` | Đăng nhập — đã nhập đủ, **Remember me đang tích** | TC_002-`6`, TC_008, TC_024 | ✅ full-page, thấy rõ dấu tích ✔ trên ô tích |
| `login_form_empty_submit_error_fullpage.png` | Đăng nhập — gửi biểu mẫu rỗng, 2 dải báo lỗi | TC_011-`a`,`b`,`c` | ✅ full-page, xác nhận **thứ tự** dải: `The Password field is required.` **trên**, `The Email Address field is required.` **dưới** |
| `login_form_wrong_credentials_fullpage.png` | Đăng nhập — sai thông tin, dải `Invalid email or password` | TC_013, TC_016, TC_017, TC_019 | ✅ full-page. ⚠️ Ô Email nền xanh nhạt = **trình duyệt tự điền**, KHÔNG phải hệ thống trả về — đây chính là bẫy mà TC_016 phải tránh bằng cửa sổ ẩn danh |
| `login_csrf_invalid_403_fullpage.png` | Đăng nhập — mã CSRF bị sửa, trang lỗi | TC_025-`a`, TC_010 | ✅ full-page, xác nhận nguyên văn `419 Page Expired!` và `Sorry, the page has expired, return to previous page and refresh to continue.` |
| `login_success_dashboard_viewport.png` | Dashboard — ngay sau khi đăng nhập, menu ảnh đại diện đóng | TC_005, TC_006, TC_022, TC_023 | ✅ viewport (cố ý — tránh chụp dữ liệu khách hàng thật). Đếm được **14** mục menu trái của Admin, đối chiếu 9 mục của PM ở TC_006 |
| `logout_menu_open_viewport.png` | Dashboard — menu ảnh đại diện **đang mở** | TC_032 | ✅ viewport, xác nhận 5 mục theo thứ tự `My Profile` → `My Timesheets` → `Edit Profile` → `Language ▸` → `Logout`, và thanh đầu trang **không** có lối Logout thứ hai |
| `forgot_password_form_default_fullpage.png` | Quên mật khẩu — mặc định | TC_027, TC_030 | ✅ full-page, xác nhận đúng 1 ô `Email Address` + nút `Confirm`, không liên kết quay lại đăng nhập |
| `forgot_password_empty_submit_error_fullpage.png` | Quên mật khẩu — gửi khi bỏ trống, dải `Email not found` | TC_028, TC_029 | ✅ full-page, xác nhận **hiện trạng lỗi** mà TC_028 sẽ bắt được |

### Vùng chưa có evidence — TC / biến thể gắn `@NeedsVerify`

| Vùng / trạng thái | TC · biến thể | Đề xuất recon bổ sung |
|---|---|---|
| Trang đăng nhập ở **viewport mobile** (`375×812`), menu thu gọn đang mở | `TC_033` | Recon riêng ở viewport mobile, chụp menu thu gọn có mục `Logout` hiển thị |
| Hộp thoại `Started tasks timers found!` khi đăng xuất lúc còn bộ đếm giờ chạy | `TC_034` | Thuộc module `TASK` (`AMB-14`) — dựng 1 task có bộ đếm giờ đang chạy rồi chụp hộp thoại, cập nhật ngược `REQ-LOGIN-30` |
| Gửi biểu mẫu đăng nhập bằng chuỗi **toàn khoảng trắng** | `TC_011-d` | Chụp dải báo lỗi thực tế để chốt hệ thống cắt khoảng trắng hay coi là có giá trị |
| Mã CSRF sau **2 lần nạp trang liên tiếp** | `TC_024` mục 🔧`2` | Đọc DOM 2 lần, ghi lại **hình thái** 2 giá trị (không ghi token) |
| Trang lỗi khi gửi mã CSRF **rỗng** | `TC_025-b` | Chụp màn hình trường hợp xoá rỗng token |
| `reset_password` với **mã người dùng không tồn tại** | `TC_031-b` | Ghi lại mã HTTP và độ dài thân phản hồi |
| Chuyển hướng `http` → `https` và header HSTS | `TC_039` | Chụp thanh địa chỉ sau khi gõ `http://`; đo lại ở tầng Network, đối chiếu bug report cũ `BUG_login_1785678750` rồi cập nhật hoặc mở bug mới |
| Đăng nhập khi **mất mạng** / **mạng chậm 3G** | `TC_040`, `TC_041` | Chụp màn hình ở hai trạng thái mạng |
| Mật khẩu có **chữ cái** để thử đảo hoa/thường | `TC_014` | Cần tài khoản test có mật khẩu chứa chữ cái (mật khẩu demo hiện là 6 ký tự số — `RISK-07`) |

---

## Bộ chạy đề xuất

| Bộ | TC | Số lượng | Thời gian ước tính |
|---|---|---|---|
| **Smoke** (`@Smoke`) | TC_001, TC_002, TC_005, TC_011-`a`, TC_013-`a`, TC_021-`a`, TC_032, TC_035, TC_036-`a` | 9 | ~12 phút |
| **Regression đầy đủ** | Toàn bộ trừ `@Slow` | 40 | ~3 giờ |
| **Chạy riêng, tách khỏi mọi bộ** (`@Slow`) | TC_026 | 1 | 65 phút |
| **Cần DevTools** (`@TechCheck`) — phần 🔧 của các TC dưới đây | TC_001, TC_002, TC_003, TC_005, TC_006, TC_008, TC_009, TC_012, TC_015, TC_016, TC_017, TC_018, TC_019, TC_023, TC_024, TC_025, TC_027, TC_030, TC_032, TC_036, TC_037, TC_039, TC_041 | 23 | ~50 phút (chỉ phần 🔧) |
| **Chờ recon bổ sung** (`@NeedsVerify`) | TC_011-`d`, TC_014, TC_024, TC_025-`b`, TC_031-`b`, TC_033, TC_034, TC_039, TC_040, TC_041 | 10 | — |

> 🔝 **Thứ tự chạy:** Nhóm B (đăng nhập thành công) chạy **đầu tiên** trong mọi bộ — `RISK-06` chỉ ra module này là cổng vào của 23 module còn lại, hỏng ở đây là chặn toàn bộ đợt kiểm thử.
>
> 🧑‍💻 **Bộ `@TechCheck` giao cho ai:** tester có kỹ năng DevTools, hoặc để automation phủ. Tester nghiệp vụ chạy **phần chính** của các TC đó bình thường và ghi `— (bỏ phần 🔧)` vào báo cáo. **Hai TC bắt buộc có người biết DevTools mới chạy được từ đầu:** `TC_025` (phải sửa mã ẩn) và `TC_041` (phải giả lập mạng chậm).
>
> ⚠️ **Báo cáo FAIL phải ghi rõ biến thể**, ví dụ `CRM_LOGIN_TC_012-c FAIL`. Ghi mỗi `TC_012 FAIL` là mất truy vết về case cụ thể.

---

## Nhật ký thay đổi

| Ngày | Nguồn | Thay đổi | TC ảnh hưởng |
|---|---|---|---|
| 2026-08-20 | Phản hồi người dùng — TC chứa quá nhiều kiểm chứng thuộc tính HTML | **Viết lại toàn bộ Test Steps và Expected Result theo ngôn ngữ người dùng nhìn thấy.** Mọi `document.title`, `body.className`, `checkValidity()`, `offsetParent`, `querySelector`, selector CSS, mã HTTP và tab Network được gỡ khỏi phần chính; nội dung cấp kỹ thuật tách xuống dòng `🔧 Ghi chú kỹ thuật (cần DevTools)` và TC gắn thêm tag `@TechCheck` (23 TC).<br>**TC ID giữ nguyên `001`→`041`** — không đánh lại, không cắt case nào, độ phủ vẫn 39/39 REQ. Bổ sung `ASM-09` giải thích vì sao `TC_008`/`TC_009` không có kỳ vọng nhìn thấy được. `TC_016` đổi cách chạy sang **cửa sổ ẩn danh + tắt tự điền** để tester bắt được lỗi mà không cần View Source. Bổ sung bộ chạy `@TechCheck` và mục "Cách đọc" phần 2.<br>Luật gốc đã đưa vào `.claude/skills/skills-rbt-manual-testing/SKILL.md` mục **Quy Tắc Ngôn Ngữ Kiểm Chứng** (+ tiêu chí 8 của Self-Quality Gate) nên lần sinh sau không lặp lại. Bản trước lưu ở `archive/test_cases_login_v2_GOP_domdanguage_20260820.md` | Toàn bộ `CRM_LOGIN_TC_001` → `CRM_LOGIN_TC_041` |
| 2026-08-20 | Yêu cầu người dùng — gộp TC nhỏ lẻ | **Sinh lại toàn bộ ở độ hạt GỘP: 82 TC → 41 TC.** Độ phủ REQ **không đổi** (39/39) và **không case nào bị bỏ**. TC ID đánh lại từ `001` — chấp nhận được vì bộ TC 82 chưa có script / execution report / RTM nào trỏ vào | Toàn bộ `CRM_LOGIN_TC_001` → `CRM_LOGIN_TC_041` |
| 2026-08-20 | `/generate-testcases-from-requirements` Mode QUICK | Khởi tạo bộ TC từ `requirements_login.md` ở **độ hạt TÁCH** — 82 TC, 3 part. Mở 9/9 ảnh evidence, không phát hiện xung đột tài liệu ↔ ảnh | (bản đầu tiên) |
